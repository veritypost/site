# Completed tasks — 2026-04-22 Session 1

## Shipped

- `b269e17` — **feat(newsroom): single-page operator workspace + audience tabs + cluster mutations** — Phase 5 redesign (post-F7) collapsing 4 prior pages into one workspace per owner vision. **Migration 126** (applied live) + rollback 127: `feed_clusters.audience` (NOT NULL CHECK adult|kid + backfill from `kid_discovery_items` membership), `feed_clusters.archived_at` + `archived_reason` + `dismissed_at` + `dismissed_by` + `dismiss_reason`, new `ai_prompt_presets` table (name+body+audience+category_id+is_active+sort_order with RLS gated on owner/admin/editor roles), 6 new RPCs (`reassign_cluster_items` / `merge_clusters` / `split_cluster` / `archive_cluster` / `dismiss_cluster` / `undismiss_cluster` — all SECURITY DEFINER + REVOKE PUBLIC + GRANT to authenticated/service_role; merge/split/move enforce audience symmetry, merge/archive idempotent via COALESCE, dismiss captures `auth.uid()` from cookie-scoped client), 3 new permissions (`admin.pipeline.clusters.manage` / `.presets.manage` / `.categories.manage` follow established `admin.pipeline.<noun>.<verb>` convention) wired into owner/admin/editor permission_sets in step 11, `bump_perms_global_version()` cache-bust at end. **Newsroom rewrite** (`web/src/app/admin/newsroom/page.tsx`): adult/kid URL-persisted audience tabs, glance bar (today's spend / runs 24h / kill switch / feed health — deep-links to /admin/pipeline/{costs,runs,settings} + /admin/feeds), filter row (dynamic category dropdown + subcategory filtered by parent_id + outlet from `feeds` + 6h/24h/72h/7d/all time window + debounced search on title+summary), prompt picker (Default editorial / Preset dropdown from `ai_prompt_presets` filtered to audience+'both' / Custom textarea), provider+model picker (existing `PipelineRunPicker` retained), Refresh feeds button (audience-scoped POST), expanded cluster cards with inline source rows (outlet from `feeds.source_name` + headline + lede + scraped time + URL link + Move-out / Move-to dropdown per row), predicted cost preview, two inline Generate buttons per card (Generate {audience} primary + cross-audience hatch "Generate kids version" only on adult tab), Merge dropdown / Split / Dismiss inline controls, "Show dismissed" toggle, recent runs strip at bottom (last 10 with retry + view-all link). **GenerationModal** (`web/src/components/admin/GenerationModal.tsx`): audience radio stripped — `audience` now required prop; new optional `sourceUrls` prop forwarded as `source_urls` to backend (kid cross-audience hatch). **3 new admin pages**: `/admin/categories` (tree editor — top-level + child rows, name/slug/color/icon/sort_order/is_active/is_kids_safe/is_premium edits, parent move with cycle prevention, soft-delete via `deleted_at`, rate-limited 30/60s, cycle prevention via ancestor walk, single-level depth cap on parent), `/admin/prompt-presets` (preset library editor — All/Adult/Kid/Both tabs, name/body/audience/category_id/sort_order, archive→is_active=false soft-delete + restore via PATCH, unique-name conflict 409), `/admin/pipeline/cleanup` (cron sweep history from `webhook_log` + manual "Run cleanup now" button via internal-fetch with CRON_SECRET — gated on `admin.pipeline.clusters.manage`, rate-limited 6/3600s). **5 new cluster mutation API routes** under `/api/admin/newsroom/clusters/[id]/`: `move-item` (reassign single discovery_item, audience symmetry enforced), `merge` (POST `{target_id}` — soft-archives source via `archived_reason='merged_into:<uuid>'`), `split` (POST `{item_ids[], title?, summary?}` — creates sibling cluster with same audience+category), `archive` (POST `{reason?}`), `dismiss` (POST `{reason?}` for dismiss + DELETE for undismiss). Every mutation: `requirePermission('admin.pipeline.clusters.manage')` BEFORE rate-limit BEFORE body-parse BEFORE RPC; PostgreSQL error code mapping (22023→422, P0002→404, audience-mismatch→409); `recordAdminAction('cluster.move'|'cluster.merge'|...)` audit logged best-effort post-mutation. **Categories CRUD** (`/api/admin/categories/{,[id]}`): POST creates with slug-format validation + 2-level depth cap; PATCH includes restore (deleted_at→null), parent reparent with cycle detection (walks ancestor chain, rejects demotion if self has children), 23505 unique-violation→409; DELETE soft via `deleted_at=now(), is_active=false`. **Presets CRUD** (`/api/admin/prompt-presets/{,[id]}`): GET list + POST create + PATCH edit + DELETE soft-via-is_active=false (idempotent); unique-on-`lower(name)`-WHERE-is_active conflict→409. **Generate route** (`/api/admin/pipeline/generate/route.ts`): added `source_urls: z.array(SourceUrlSchema).max(10).optional()` to body schema; when present + `cluster.audience='adult'` AND `audience='kid'` (cross-audience hatch only), synthesizes virtual `DiscoveryItem`s (id `override:<url>`, no feed_id) and skips discovery_items SELECT + audience-feed peek + state='generating' update + state-reset finally. Adversary-applied guard: rejects 422 if `cluster.audience !== 'adult'` OR `audience !== 'kid'`. Cluster row select extended with `audience`. Cost cap enforcement unchanged (runs at L491-533 before override branch). **Cron** (`/api/cron/pipeline-cleanup/route.ts`): added 4th sweep — archives `feed_clusters` WHERE `archived_at IS NULL AND locked_at IS NULL AND created_at<now()-14d AND id NOT IN (articles+kid_articles cluster_ids ANY status)` AND `generation_state IS DISTINCT FROM 'generating'` (adversary-applied guards prevent racing active generation); cap 500/run; per-row `archive_cluster(id, 'auto_expired_14d')` RPC; counters `clusters_archived` + `cluster_expiry` errors map. **Ingest route** (`/api/newsroom/ingest/run/route.ts`): accepts optional `{audience: 'adult'|'kid'}` body — when set, filters `feeds` query + persists in `pipeline_runs.input_params.audience` + skips unused-audience clustering pass (zero-filled summary preserves response shape). `feed_clusters` insert payload now writes `audience` from source feed (typed after migration regen). **Feeds page** (`/admin/feeds/page.tsx`): All/Adult/Kid tab toggle URL-persisted via `?audience=...`, audience badge on every row, audience required dropdown in Add modal seeded from active tab, stats + counts scope to active tab. Audience editing post-create flagged for follow-up (PATCH route doesn't accept yet). **Cluster detail page** (`/admin/newsroom/clusters/[id]/page.tsx`): replaced 25KB client component with 22-line server-component redirect to `/admin/newsroom?cluster=<encoded-id>`. **Admin hub** (`/admin/page.tsx`): refreshed `@admin-verified 2026-04-22`, Content Pipeline section collapsed (Newsroom primary + 3 deep-dive pipeline links + Categories + Prompt presets + Feeds + Articles + Cleanup), broken Story Manager + Kids Story Manager links removed (Stage 0 recon verdict: snapshot-era, REWRITE >400 LOC needed for F7 schema; F7-native `/admin/articles/[id]/{review,edit}` is canonical editor). **6-stream parallel build pattern** validated: Stream 1 (clustering writes audience), Stream 2 (categories editor), Stream 3 (presets editor), Stream 4 (5 cluster mutation routes), Stream 5 (feeds tabs), Stream 6 (Newsroom rewrite + GenerationModal + admin hub) all dispatched in parallel against locked migration 126 spec; Stream 7 (cron sweep + cleanup view), Stream 8 (source_urls plumbing + cluster detail redirect + perm-set wiring follow-up) ran in second wave. Final independent adversary verdict: 8/10 GREEN, 2 YELLOW with applied fixes (P1 source_urls cluster.audience guard + P1 cron locked_at guard), 0 RED unresolved (P0 migration application closed by owner paste). **Migration column-name + perm-key + RLS join + trigger function bugs** caught + fixed during paste cycle: `r.slug`→`r.name`, `label`→`display_name`, `category_key`→`category`, `pipeline.manage_*`→`admin.pipeline.<noun>.<verb>`, `touch_updated_at`→`update_updated_at_column`, `permission_sets.name`→`.key`. **Sweep**: 11 routes updated to canonical perm keys; types regenerated post-migration; all `as never` / `as unknown as { from }` / `looseSupabase` casts marked `// types regen pending migration 126` removed. Verification: `npx tsc --noEmit` green; `npx next lint` clean (only pre-existing feeds warning); zero leftover migration-pending markers. **Open follow-ups**: owner xlsx reconcile for 3 new perm keys (`/Users/veritypost/Desktop/verity post/permissions.xlsx`); owner click-through verification of new Newsroom workspace + Categories + Prompt Presets + Cleanup pages on Vercel deploy; STATUS.md cron line refresh from "every 5 min" to daily + 4 sweeps. 24 files changed, +6894 / -1664. Pushed to origin/main.
- `bb48426` — **chore: web/.gitignore for .vercel local-link metadata** — Vercel CLI created `web/.vercel/` during `vercel link`; folder holds local-only project link + cached build artifacts; CLI auto-generates this `.gitignore` at link time. Committed for cleanliness.
- `d49a5aa` — **Stream B finishing — captureWithRedact in remaining 2 F7 routes + clear baseline lint warnings** — Wired `captureWithRedact` into `runs/[id]` GET handler + `newsroom/ingest/run` route (Stream A's `6132de7` already covered cancel/retry/generate/redact.ts itself). Lint cleanup: removed unused `useEffect` from `Toast.jsx` + `Toast.tsx` (defect twin); `_`-prefixed `allPermsFetchedAt` (`permissions.js`) + `flushTimer` (`track.ts`) per repo's `/^_/u` exception rule. F7-route grep verified zero raw `Sentry.captureException` remain. 145 baseline warnings on launch-hidden pages out of scope.
- `6132de7` — **Stream A — F2 + P2-A + Task 16 stash drop (3 deferred follow-ups)** — F2: `rewriteForPlagiarism()` in `lib/pipeline/plagiarism-check.ts` gained optional `additionalInstructions?: string` param; route passes `promptOverrides.get('plagiarism_check')` so the 11th LLM call now receives Layer 1 customization. P2-A: generate route's main finally `pipeline_runs` UPDATE was already `.eq('status','running')`-guarded; only stale comment in cancel route header rewritten to reflect the symmetric guard. Task 16 follow-up: dropped legacy `output_summary.error_type` + `final_error_type` stash from generate finally + cancel route output_summary; updated retry route to read `pipeline_runs.error_type` column directly (was the last consumer of the stash). `failRun` no longer writes `output_summary` (already migrated to dedicated column). Stream A also bundled in Stream B's mid-flight `redact.ts` helper + Sentry swap on cancel/retry/generate routes (couldn't surgically split file-level edits).
- `1cdbadd` — **fix(deploy): drop pipeline-cleanup cron to daily (Vercel Hobby tier limit)** — Every push since Task 19 (`9b9a32e`) had been failing the Vercel build with "Hobby accounts are limited to daily cron jobs. This cron expression (*/5 * * * *) would run more than once per day." E4 assumption (Pro tier based on 9 daily crons) was WRONG — Hobby allows multiple daily crons, just not per-minute frequency. Schedule changed from `*/5 * * * *` to `0 6 * * *` (daily 6 AM UTC). Trade-off: orphan-cleanup latency goes from 5min to ~24h (acceptable; cluster locks self-expire via 10-min RPC TTL anyway). Route TSDoc documents how to restore per-minute when owner upgrades to Pro. **This commit unblocked all of Phase 2/3/4 from actually deploying — production had been frozen at commit 271e3d7 for hours waiting on this fix.**
- `f5be651` — **F7 Phase 4 Task 29 — pipeline settings UI /admin/pipeline/settings** — `web/src/app/admin/pipeline/settings/page.tsx` (542 lines, sha256 `3c5044f94c04db77e835c6bf08ea1471c9a171febce089b913051a950019e343`). NEW directory + file. NO new API endpoint — reuses existing `PATCH /api/admin/settings` at `web/src/app/api/admin/settings/route.js` (body `{key, value:string}`, enforces is_sensitive block, validates number regex + boolean literal, writes audit_log). Four `PageSection` groups: kill switches (3 toggles for `ai.ingest_enabled`, `ai.adult_generation_enabled`, `ai.kid_generation_enabled`); cost caps (3 numeric inputs for `pipeline.daily_cost_usd_cap`, `pipeline.per_run_cost_usd_cap`, `pipeline.daily_cost_soft_alert_pct`); cluster + story-match + plagiarism thresholds (5 numeric inputs); default category dropdown (live `categories` table read). Per-row Save button mirroring admin/settings pattern (no bulk atomic save) — single PATCH per key, dirty gate, toast on success/error, `busyKey` single-slot disables other Saves while one is in flight. Boolean serialization: bare `'true'`/`'false'` strings (endpoint regex requires literal). Default category sent RAW UUID (not JSON-wrapped) to preserve stored bare-UUID shape — `unwrapString()` helper handles JSON-quoted fallback for forward compat. ADMIN_ROLES client gate. Missing-keys warning banner with link to `/admin/settings` for fallback. All 12 setting keys MCP-verified present + value_type confirmed. No new deps.
- `1f19e42` — **F7 Phase 4 Tasks 23 + 24 + 25 — article review + edit + publish/reject flow** — `web/src/app/api/admin/articles/[id]/route.ts` (extended existing T-005 route with PATCH/Zod/audit), `web/src/app/admin/articles/[id]/review/page.tsx` (NEW), `web/src/app/admin/articles/[id]/edit/page.tsx` (NEW). Total +2228 / −43 across 3 files. **Task 23 review page**: GET fetches article + sources + timeline + quizzes via the new API; renders title/subtitle/excerpt/status/moderation badges + body_html via `dangerouslySetInnerHTML` (server-sanitized via DOMPurify); shows correct quiz answer to admin reviewer (reads `metadata.correct_index`); action buttons Edit (nav), Regenerate (POST `/api/admin/pipeline/generate` with article's cluster_id+audience), Publish (PATCH status=published — disabled when already published), Reject (modal with retraction_reason textarea). **Task 24 edit page**: plain `<textarea>` for body markdown (PATCH server runs `renderBodyHtml` to recompute body_html — no rich-text editor dep added); repeatable inline editors for sources (title/url/publisher/author/date), timeline (date/label/body/source_url), quizzes (question/options 2-8/correct_index radio/explanation); add/remove rows; client-side validation; full-set POST so server's delete+reinsert is authoritative. **Task 25 publish/reject (no separate route)**: both PATCH on shared endpoint. Publish sets `status=published, published_at=now(), unpublished_at=null, retraction_reason=null, moderation_status=approved`. Reject sets `status=archived, unpublished_at=now(), retraction_reason=<reason>, moderation_status=rejected`. Strict transition whitelist: `draft→{published,archived}`, `scheduled→{published,archived,draft}`, `published→archived`, `archived→draft` — direct `published→draft` rejected as 400. **PATCH endpoint**: `admin.articles.detail.view` for GET; PATCH dynamically requires `admin.articles.edit.any` and/or `admin.articles.publish` / `admin.articles.unpublish`; outrank guard via `requireAdminOutranks(prior.author_id, actorId)`; rate limit `admin_article_edit:user:<actor>` 30/60s; Zod `.strict()` on body + every nested object; `correct_index` validated against `options.length`; body markdown re-rendered server-side via `renderBodyHtml`; sources/timeline/quizzes are delete-and-reinsert when array present, omitted = leave-as-is, empty = clear-all; quiz `correct_index` stashed in `metadata.correct_index` (DB `options` jsonb only carries `{text}` — never leaks answer key); audit emits up to 2 rows (`article.edit` + `article.publish`/`article.unpublish`); idempotent no-op when patch matches current state (no audit, no write). Audience routing detected by which table holds the row (`articles` vs `kid_articles`). All 5 perms MCP-verified. tsc + lint clean. No new deps.
- `b250695` — **F7 Phase 4 Task 22 — generation modal (replaces inline Generate buttons)** — `web/src/components/admin/GenerationModal.tsx` (NEW, 907 lines post-prettier) + `web/src/app/admin/newsroom/page.tsx` (net −32) + `web/src/app/admin/newsroom/clusters/[id]/page.tsx` (net −41). Reusable component (2 identical call-sites with material drift risk) chosen over inline per CLAUDE.md spirit; one Generate button per cluster now opens the modal with audience radio + freeform instructions textarea (2000-char cap matches Zod). **Key investigator finding**: `/api/admin/pipeline/generate` is fully synchronous (`maxDuration=300`) — response doesn't return until pipeline finishes, so brief's "POST returns run_id then poll" design was wrong. Solved with **fire-and-forget + discovery poll**: POST is not awaited; modal polls `pipeline_runs` WHERE `cluster_id=? AND audience=? AND pipeline_type='generate' AND started_at >= openedAt` ordered desc limit 1 every 2s until the run row (inserted at generate L522 before any LLM work, ~50ms in practice) appears; then switches to Task 12 GET `/api/admin/pipeline/runs/:id` polling. Fire-and-forget promise still resolves — early-exit failures (kill switch 503 / cost cap 402 / rate limit 429, where no run row is ever created) set `phase='error'` with the response body's `error_type` + `error` fields. Discovery timeout = 30s. Phases: `form → starting → discovering → polling → {completed|failed|error}`. On `completed` + `article_id`: 600ms delay (user sees "Completed" badge) then `router.push('/admin/articles/:id/review')` (Task 23 404 acceptable scaffold per prompt). On `failed|cancelled`: surfaces error_type + error_message with Retry (Task 17) or Close; retry spawns new run and re-enters polling directly against `new_run_id` (skips discovery). Cancel (Task 18) soft-handles 409 races. Step timings bar: each `pipeline_costs` row as colored pill (green success / red failure) with tooltip (step + latency + error_type). Totals row (cost / latency / retries / failures) consumes Task 12's `totals` payload. Polling cleanup: single `useRef<ReturnType<typeof setInterval>>` cleared in 6 places — `open=false` transition, component unmount, phase→`completed|failed|error`, discovery timeout. Dirty-close gate: mid-run close fires `window.confirm` warning that the backend lambda continues (it does). Radio group hand-rolled (admin kit has no Radio component). No new deps. No emojis. `'use client'`. Imports via `@/components/admin/...` aliases — relative paths triggered TS `forwardRef<any>` inference collapse on `.jsx` targets in the same dir (unexpected; documented in code comments). **Pre-existing tsc errors in `api/admin/articles/[id]/route.ts` + `admin/articles/[id]/edit/page.tsx` are parallel-agent Task 23/24 work, NOT regressions** — `git diff --stat` confirms zero touch. Full 3-agent internal flow: investigator (7 reference files + all admin-kit prop signatures) → adversary (caught synchronous-POST architecture + designed discovery-poll; dismissed brief's 2s-polling concern against Task 12's 15s maxDuration ceiling; validated reusable-vs-inline tradeoff; verified cleanup coverage, Retry/Cancel race handling, error_type taxonomy isn't leaking internals) → implementer (prettier reflow via lint-staged on commit — structural only, no logic change). Self-verified: `tsc --noEmit` exit 0 for the three touched files, `next lint --file` clean on all three, no emoji, no `as any`, no `@ts-expect-error`. Did NOT click-through browser — runtime needs owner dev-server eyeball.
- `a53a260` — **F7 Phase 4 Task 27 — run detail view /admin/pipeline/runs/:id** — `web/src/app/admin/pipeline/runs/[id]/page.tsx` (948 lines post-prettier, sha256 `181289be1722224024b44ee5c69d5379c19da6c161a9c34e2a8bcb85f138f6bc`). NEW `[id]/` directory. Single client-component page consuming Task 12 GET `/api/admin/pipeline/runs/:id` (shape `{ok, run, steps, totals}`). ADMIN_ROLES client gate mirrors Tasks 20/21/26 — supabase.auth.getUser → user_roles join → ADMIN_ROLES.has(name) → redirect on fail. UUID shape-check before fetch. 404-branch + load-error branch + not-found branch all render with back-to-pipeline CTA. Layout: header (8-char id title + pipeline_type/relative times subtitle + breadcrumb back to /admin/pipeline + action buttons); info block (status+audience+pipeline_type+model+provider badges, then 2-col grid of full run_id/started/completed/duration/triggered_by/cluster_id); error panel (danger-bordered, shown when run.error_message present); Totals (9-tile stat grid — cost, steps, in/out tokens, cache read/creation tokens, cache hit ratio, retries, failures with danger emphasis when >0); Step timings (CSS flex bar chart, widthPct = latency_ms / max * 100 clamped to 1%, accent for success, danger for failure, label + monospace duration); Step detail (hand-rolled `<table>` with Step/Model/Provider/Input/Output/Cost/Latency/Status columns; inline error row spanning colspan=8 when !success); Input params + Output summary (two `<pre>` blocks, `JSON.stringify(v, null, 2)`, overflow:auto, maxHeight 480, monospace); Freeform instructions `<pre>` (only when present); Prompt fingerprint monospace single line. Action buttons gated: **Retry** (visible iff `status='failed' AND pipeline_type='generate'`) → POST Task 17, on success `router.push('/admin/pipeline/runs/:new_run_id')`; **Cancel** (visible iff `status='running'` — guard does NOT require pipeline_type=generate in UI since backend enforces) → POST Task 18, reloads on success; **Refresh** (always visible, manual reload — spec explicitly said no auto-poll; Task 22 modal owns polling); **Open cluster** (when `cluster_id` present, links to Task 21); **Back to pipeline**. Hand-rolled helpers inline: `relativeTime()`, `absoluteTime()`, `formatCost()` ($N.NNNN <0.01 / $N.NNNN normal), `formatInt()` (toLocaleString), `formatDuration()` (ms/s/m s), `formatPercent()`, `statusVariant()`, `safeStringify()` (try/catch fallback '[unserializable]'). No charting lib, no date-fns, no new deps. No emojis, no `as any`, no `@ts-expect-error`. Json type imported from `@/types/database` (not re-exported from database-helpers). Toast feedback on all mutations. tsc clean (0 new errors — pre-existing GenerationModal.tsx errors unchanged), `npx next lint --file ...` returns "✔ No ESLint warnings or errors". Lint-staged/prettier reformatted from ~720 hand-written lines to 948. Unblocks Task 20 generate-success redirect + Task 21 history-link + Task 22 modal. One git complication: an earlier attempt picked up a sibling agent's untracked `pipeline/settings/page.tsx` via lint-staged auto-add; `git reset --soft HEAD~1` + re-stage of only this file produced the final clean single-file commit.
- `d366911` — **F7 Phase 4 Task 26 — observability dashboard /admin/pipeline/runs** — `web/src/app/admin/pipeline/runs/page.tsx` (497 lines, sha256 `63508169fb6868258d228a9cc1b9988e47e90745b3c5ac20500e789a65a60081`). NEW directory + file. NO migration, NO new perm (ADMIN_ROLES client gate matching Task 20/21; pipeline_runs RLS already admin-only). Single client-component file, `ToastProvider`-wrapped inner; auth verbatim from newsroom (supabase.auth.getUser → user_roles join → `ADMIN_ROLES.has(name)` → redirect on fail). Data: `pipeline_runs.select('id, pipeline_type, status, audience, cluster_id, total_cost_usd, duration_ms, started_at, error_type').order(started_at desc).range(offset, offset+49)` + chained `.eq()` per filter + `.gte('started_at', cutoff)` for date range. Hand-rolled `cutoffForRange()` returns ISO string for 24h/7d/30d or null for 'all'. 4 Select dropdowns: status (all/running/completed/failed), audience (all/adult/kid), pipeline_type (all/ingest/generate), date range (24h/7d/30d/all, default=7d). Hand-rolled table (not DataTable — DataTable has built-in 25/50/100 page-size, spec required Load more at 50): Run (8-char id), Type, Status badge (success/info/danger), Audience badge (info/success/neutral or em-dash for null), Cluster (8-char or em-dash for null), Cost ($N.NNN or $N.NNNN if <$0.01), Duration (ms/s/m formatting), Started (relative time with title=ISO tooltip), Error (error_type truncated 24 chars, shown only when status=failed, em-dash otherwise). Row click → `router.push('/admin/pipeline/runs/:id')` (Task 27 — not built; 404 acceptable). Offset-based pagination: 50 per page. **P1 (adversary fix): useEffect on `[authorized, statusFilter, audienceFilter, typeFilter, dateRange]` resets offset=0 and calls `load(true, 0)`; without this, changing filters leaves stale offset and shows wrong page.** `load(reset, startOffset)` takes explicit offset param (not state-dependent) to avoid stale-closure bugs. Adversary verified: `pipeline_runs` has no index on `started_at` but indexes exist on status, pipeline_type, cluster_id; filtered queries hit those first. Daily volume low (admin-only table) — index deferred as optimization, not blocker. Row hover uses canonical `ADMIN_C.hover` token (not ad-hoc rgba). No `DataTable` to sidestep its pagination; no `Link` wrapping needed (client-side `router.push` on row click). Coexists with existing `/admin/pipeline/page.tsx` shell per scope. TSDoc documents coexistence + Task 27 dep. Self-verified: tsc exit 0 (pre-existing GenerationModal.tsx errors unrelated), lint clean, no emoji, no `as any`/`@ts-expect-error`. Did NOT click-through browser — runtime/visual needs owner eyeball on dev server. Net delta: +497 new file. 1 investigator + 1 adversary + 1 implementer (3-agent flow internalized).
- `2d63621` — **F7 Phase 4 Task 21 — cluster detail page /admin/newsroom/clusters/:id** — `web/src/app/admin/newsroom/clusters/[id]/page.tsx` (711 lines, sha256 `592a4f9b09b061a899dc7f8428a48d93c4e252afa1985befe72de9fa998ce1e0`). NEW directory. Client-component page inside `ToastProvider`, ADMIN_ROLES gate mirrors Task 20. UUID shape-check short-circuits bad ids to EmptyState (avoids 22P02). Parallel `Promise.all` loads cluster + adult discovery_items + kid_discovery_items + pipeline_runs (limit 20); sequential single-row categories lookup for category name. Audience inferred from item counts (cluster carries no audience column). Three PageSections: cluster header (title, summary, Breaking + Locked badges, relative time, category, updated time), discovery items grid (merged adult/kid with audience source badge, state badge, outlet from metadata.outlet, external link to raw_url), generation history list (status + audience badges, 8-char run_id prefix, model, cost, duration, relative time, error message on failure, nav link to `/admin/pipeline/runs/:id` — Task 27 not yet built so 404 acceptable). Header actions: Generate adult / Generate kid, Unlock (locked-only, POST Task 11 endpoint), Back to newsroom. Hand-rolled `relativeTime()` + `truncate()` inline (date-fns not installed). No emojis, no `as any`, no `@ts-expect-error`. TSDoc documents STAGED dependencies and deferred stretch items. Full internal 3-agent flow; 711 lines vs Task 20's 474 because 3 distinct UI sections. Self-verified: tsc exit 0, lint clean. Unblocks the View button on Task 20.
- `9aca4e6` — **Docs housekeeping: STATUS.md refresh + .gitignore Future Projects HTML** — `Reference/STATUS.md` gained F7 AI pipeline row in platforms table + 4 F7-related rows in key-machinery table (lib/pipeline/*, ingest route, generate route, cleanup cron); softened "114 tables" to "100+ (use MCP for live count)". `.gitignore` hides `Future Projects/*.html` scratch mockups.
- `2a45c11` — **Owner-decision defaults + session artifact archive + refreshed next-session prompt** — E1 (F7-DECISIONS §8 quiz_verification spec reconciled to match code's throw-and-regenerate), E3 (gutted stale FIX_SESSION_1 F7 entry; pointed at F7-DECISIONS-LOCKED.md as canonical tracker), E5 (ratified Option A inline-in-ingest, already shipped 238045b), E6 (retry internal-fetch kept). Deferred E2 (redact.ts — Phase 4 follow-up) + E4 (Vercel tier assumed Pro). Archived `NEXT_SESSION_PROMPT.md` → `_superseded/`, 7 FACTS sheets → `_facts-archive/`. Wrote fresh `NEXT_SESSION_PROMPT.md` for Phase 4 Tasks 22-30. Committed `MASTER_CLEANUP_PLAN.md`.
- `82bbf19` — **F7 cleanup — 5 deferred as-never casts cleared post migration 120 apply** — `generate/route.ts` × 2 + `runs/[id]/cancel/route.ts` + `cron/pipeline-cleanup/route.ts` UPDATE casts + output_summary `as unknown as Json` downgraded. `src/types/database.ts` regen included. Final `as never` count: 0 across touched files. Two legitimate Json-bridges remain (persist-article RPC payload + generate's DiscoveryItem narrow).
- `27145e3` — **F7 cleanup Stream 3 — dead-cast sweep + observability columns + kids_summary type drop** — Cast removals across 6 files (D1-D11 partial — 5 deferred to 82bbf19). Observability: `call-model.ts writePipelineCost` now writes dedicated `cache_read_input_tokens`/`cache_creation_input_tokens`/`cluster_id`/`error_type`/`retry_count` columns (were stuck at 0 default). Bare `console.log` → `pipelineLog.info` per runbook §3. `Retry-After` header derives from `rl.windowSec`. Type: `PersistArticlePayload.kids_summary` dropped (migration 124 removed the dead RPC branch). Closes D1-D11 + F4 (code side) + F7 + F10 + F11.
- `e4d11e9` — **F7 cleanup Stream 4 — docs sync (6 files)** — `STATUS.md` TODO→FIX_SESSION_1; `CLAUDE.md` tree gains `pipeline/` subdirectory; `F7-DECISIONS-LOCKED.md` drops duplicate Task 3 stub + flips Task 4 → SHIPPED + phase rollup blocks; `F7-PHASE-3-RUNBOOK.md` rewrites scope + replaces 10-agent vocab with canonical 12-step + changelog entries for Tasks 11/16/17/18/19; SESSION_LOG + COMPLETED_TASKS staged section updated. Closes DOC1-DOC6.
- `238045b` — **F7 cleanup Stream 1 — clustering orchestration wired into ingest (closes F1)** — `web/src/app/api/newsroom/ingest/run/route.ts` +200 LOC: per-audience `clusterAudience()` helper runs `preCluster` + `findBestMatch` over last 6h pending items; match → `state='ignored'`, no match → new `feed_clusters` row + items flip to `state='clustered', cluster_id=<new>`. Singletons stay pending for 24h retention. `story-match.ts` +20 LOC: new `loadKidStoryMatchCandidates()`. **Load-bearing fix** — before this, cluster.ts + story-match.ts had ZERO callers and the entire pipeline was unreachable through normal flow. `output_summary` gets `clusteringAdult`/`clusteringKid` blocks; `step_timings_ms` gets `cluster_<aud>_ms` + `story_match_<aud>_ms`.
- `f26da57` — **F7 cleanup prep — staged migrations 122 + 124** — `schema/122` adds 5 missing cluster_id FK constraints (discovery_items, kid_discovery_items, pipeline_runs, pipeline_costs → SET NULL; kid_articles → CASCADE) + flips `feed_clusters.locked_by` from NO ACTION → SET NULL + 2 supporting indexes. `schema/124` CREATE OR REPLACE persist_generated_article RPC with the dead `kids_summary` UPDATE branch removed (kid_articles has no such column). Rollback files 123 + 125 included. Both applied live by end of session.
- `8dc121b` — **F7 Phase 3 cleanup Stream 2 — F3 + F5 + F6 + F8 + F12 (5 surgical fixes, 1 commit)** — `web/src/app/api/admin/pipeline/generate/route.ts` (+63 / -44 net +19). F3 audience peek now fail-closed at 422 with `error_type='schema_validation'` when `feedIds.length===0` after null-filter (prevents adult-tagged cluster passing as `audience='kid'`); the existing `if (feedIds.length>0)` wrapper unwrapped — branch now unconditional after the guard. F5 `statusForError` switch loses 3 dead cases (`permission_denied`/`rate_limit`/`kill_switch` — all return their HTTP status before any `pipeline_runs` row is written, never round-trip the switch); same 3 cases removed from `safeErrorMessage`; comment block added above each function explaining why the codes are absent. F6 `QuizQuestionSchema` gets a `.refine()` rejecting questions with **>1** is_correct=true options (deviation from prompt's `===1`: zero is allowed because L1291 normalizer falls back to `correct_index`/`correct_answer`; `===1` would break legitimate `correct_index`-only LLM outputs — this hits the actual bug while preserving the existing fallback path). F8 main-finally `pipeline_runs` UPDATE adds `.eq('status','running')` chain so cancel/cron-orphan-cleanup terminal states win the race; comment explains the guard. F12 `step_timings_ms` + `output_summary` casts simplified `as unknown as Json` → `as Json` (types already match Json). Investigator → adversary → implementer flow internal: adversary caught the F6 deviation (zero-is_correct path with correct_index fallback) and recommended `<=1` over `===1`. Self-verified: `tsc --noEmit` exit 0, `next lint --file route.ts` clean, no other consumers of `QuizQuestionSchema`/`QuizSchema` outside route.ts (grep confirmed). Closes F3, F5, F6, F8, F12 from `Sessions/04-22-2026/Session 1/MASTER_CLEANUP_PLAN.md`.
- `64cd609` — **M1-M47 multi-agent review sweep** — 31 applied fixes across `VerityAdMockups.jsx` (moved to `Current Projects/ad-mockups/`), `Reference/CLAUDE.md`, `Current Projects/F7-PM-LAUNCH-PROMPT.md`; 7 NO-CHANGE; 3 SUBSUMED; 4 DEFER-OWNER; 4 DEADLOCKED (logged). New root `STATUS.md` symlink. `REVIEW_UNRESOLVED_2026-04-21.md` session artifact.
- `c043b2d` — **M6 kids-waitlist email capture (code + staged migration)** — `schema/112_kids_waitlist.sql` + `schema/113_rollback_kids_waitlist.sql` (RLS + rate-limits seeds, idempotent `ON CONFLICT DO UPDATE`), `web/src/app/api/kids-waitlist/route.ts` (dual-key rate limit + honeypot + bot UA + structured log taxonomy), `web/src/app/kids-app/page.tsx` inline form with aria-live. Migration apply pending.
- `df7b598` — **F7 Phase 1 — decisions locked + editorial-guide.ts port (9/10 exports)** — `Current Projects/F7-DECISIONS-LOCKED.md` (8 decisions + 10 invariants + 14 pre-flight items + 16 divergences + 5 open items + 7 future obligations, rev 2 + 5 clarifications after 3-auditor review), `web/src/lib/pipeline/editorial-guide.ts` (verbatim port, sha256 in TSDoc, full F7 PM §3a four-agent flow complete), `web/.prettierignore` (permanent `src/lib/pipeline` exclusion).
- **F7 Phase 1 Task 2 — call-model.ts + cost-tracker.ts stub + SDK installs** (SHIPPED `4c73c17`) — `web/src/lib/pipeline/call-model.ts` (437 lines, multi-provider Anthropic + OpenAI wrapper, abort-aware sleep, finally-block pipeline_costs write with all NOT NULL cols populated on every path, 60s pricing cache, Anthropic prompt caching, retry envelope 3×[1000,4000,15000]ms ±20% jitter), `web/src/lib/pipeline/cost-tracker.ts` (44-line stub, Task 3 replaces), `web/.env.example` (ANTHROPIC_API_KEY= added), `web/package.json` (@anthropic-ai/sdk@^0.90.0 + openai@^6.34.0). Full F7 PM §3a four-agent flow: Agents 1+2 parallel investigators → Agent 3 reviewer resolved 8 open questions → Agent 4 adversary caught 2 real correctness bugs (abort-unaware sleep + NOT NULL column omissions) → Agent 5 Impl → post-impl VERIFY SHIPPED + REGRESSION CLEAN. 6 new tsc errors expected (ai_models table-not-in-types until Task 3 regenerates).
- **F7 Phase 1 Task 3 — migration 114 + full cost-tracker + errors.ts refactor** (SHIPPED `44b5a46`; migration applied live `fyiwulqphgmoqullmrfn`) — `schema/114_f7_foundation.sql` (651 lines, BEGIN/COMMIT wrapped — 8 new tables, 5 ALTERs with backfill from metadata JSONB, 21 RLS policies including RESTRICTIVE kid-JWT blocks on articles/sources/timelines/quizzes, RPC `pipeline_today_cost_usd` UTC-day SECURITY DEFINER, trigger fn `tg_set_updated_at` attached to 8 tables, seeds 19 settings + 4 ai_models + 2 rate_limits), `schema/115_rollback_f7_foundation.sql` (128 lines, idempotent), `web/src/lib/pipeline/errors.ts` (58 lines, 5 classes lifted out to break circular import), `web/src/lib/pipeline/call-model.ts` (refactored: classes removed + back-compat re-export), `web/src/lib/pipeline/cost-tracker.ts` (203 lines, replaces Task 2 stub — RPC-backed today-sum, 60s cached settings, fail CLOSED on DB error with cap_usd=-1 sentinel). Full F7 PM §3a four-agent flow: Agents 1+2 deeply verified live schema via SQL → Agent 3 resolved 14 open questions → Agent 4 caught 8 real correctness bugs (cache col names, schema-qualification, NOT NULL DEFAULT, trigger fn, BEGIN/COMMIT, Haiku date-stamp, UNIQUE index, RPC timezone) → Agent 5 absorbed all 8 fixes + 3 honest deviations → post-impl VERIFY SHIPPED + REGRESSION CLEAN.
- **F7 Phase 1 Task 4 — exit verification + admin/feeds fix** (SHIPPED `958879c`) — migration 114 applied by owner via Supabase SQL editor; `npm run types:gen` regenerated `web/src/types/database.ts` with 8 new tables + RPC typed; `web/src/app/api/admin/feeds/route.ts` gained `audience` field (required by migration's `feeds.audience NOT NULL`); `Sessions/04-22-2026/Session 1/PHASE1_EXIT_RLS_PROBE.sql` written for owner's manual live-behavior probe (MCP can't `SET ROLE authenticated`). Independent gate auditor cleared Phase 1 → Phase 2 exit: 13/13 checks passed against live DB + on-disk files. Pushed to origin/main 2026-04-22.
- **F7 Phase 2 Task 5 — clean-text.ts port** (SHIPPED `12877ca`) — `web/src/lib/pipeline/clean-text.ts` (43 lines, 2167 bytes). Verbatim port of 12-replace HTML/markdown stripper from snapshot's `api/ai/pipeline/route.js` L51-67. sha256 `a9540599df547a3c4b98f4015fc05ebf1061dbf6576587dfe59cf34e7edac82c` baked into TSDoc for tamper-evidence. Full F7 PM §3a four-agent flow: Agents 1+2 returned IDENTICAL gameplans (same source quote, same sha256, same typing) → Agent 3 locked execution plan → Agent 4 adversary caught Agents 1/2/3 miscount (13 replaces claimed vs actual 12) + `tsx` not installed → Agent 5 implemented verbatim → post-impl VERIFY SHIPPED + REGRESSION CLEAN. Preserved snapshot quirks documented in TSDoc (`/---+/g` strips mid-prose hyphens, `<!--.*?-->` no /s flag, bold-before-italic order-dependent).
- **F7 Phase 2 Task 6 — scrape-article.ts port + cheerio dep** (SHIPPED `49904f3`) — `web/src/lib/pipeline/scrape-article.ts` (118 lines). Verbatim port of Jina Reader + cheerio fallback with 200-char floor / 10000-char ceiling; silent-fail design per snapshot. sha256 `0b87740a3327978fdf6a4ee462bf6bebabdd35a3420d5d63fe7693ef684593d2` in TSDoc. Preserved precedence quirk on L28 (snapshot comment). Full 4-agent flow with Agent 4 adversary catching cheerio version pinning.
- **F7 Phase 2 Task 7 — cluster.ts (35% keyword overlap)** (SHIPPED `da6c34e`) — `web/src/lib/pipeline/cluster.ts` (168 lines). Exports: `STOP_WORDS`, `ClusterInputArticle`, `Cluster`, `ClusterResult`, `extractKeywords`, `keywordOverlap`, `preCluster`, `getClusterOverlapPct`. sha256 `dfae072d132c51ecb507c58c9e54c967b2b9d975171fe2020ed417c75755303f`. Greedy first-fit-best-score clustering, `|A∩B|/max(|A|,|B|)` raw-length overlap. Preserved `STOP_WORDS` has 'been' twice (snapshot drift, intentional). Full 4-agent flow.
- **F7 Phase 2 Task 8 — story-match.ts (40% overlap dedupe)** (SHIPPED `d50bc20`) — `web/src/lib/pipeline/story-match.ts` (163 lines). Exports: `STORY_MATCH_CANDIDATE_LIMIT=200`, `StoryCandidate`, `StoryMatchResult`, `findBestMatch`, `loadStoryMatchCandidates`, `getStoryMatchOverlapPct`. Validator-corrected signature: `findBestMatch(clusterKeywords, candidates, thresholdPct)` (not title+body re-extraction — snapshot L140 uses pre-computed `cluster.keywords` directly). Schema drift handled: queries `articles` (not `stories`), coalesces `seoKeywords → tags → extractKeywords(title)`, `status='published'` only. Full 4-agent flow; Agent 3's self-flagged bug confirmed by fresh validator before revision.
- **F7 Phase 2 Task 9 — /api/newsroom/ingest/run route** (SHIPPED `98920a7`) — `web/src/app/api/newsroom/ingest/run/route.ts` (389 lines, sha256 `a1f0112926f7ff359cce3653d55d6725c6fcc8c7ca4b75b42326fb16566fe018`). POST handler for admin "Refresh feeds" button. Full Promise.allSettled RSS fanout (5s parser timeout + 6s race, ported verbatim from snapshot L8-21), splits by `feeds.audience` → `discovery_items` / `kid_discovery_items`, dedup in 100-URL batches, 500-row upsert chunks, 24h pubDate filter. Scraping+clean-text deferred to Phase 3 Task 10 per snapshot L287. Permission: `admin.pipeline.run_ingest` (6 role assignments live). Kill switch: `settings.ai.ingest_enabled` (60s cached, fail-closed). Rate limit: `newsroom_ingest` policy (5/600s from DB). Pipeline_runs row with status=running → completed|failed, duration_ms, items_processed/created/failed, output_summary JSON. `record_admin_action` audit on success. Sentry capture on all DB failures. `rss-parser@^3.13.0` added. Full F7 PM §3a four-agent flow: Agents 1+2 investigators → Agent 3 reviewer → Agent 4 adversary caught 6+ REAL bugs (non-existent `feed_items` table, wrong setting key, bogus pipeline_runs columns, hardcoded rate-limit, missing audit log, scrape/clean-text misplaced, no audience branching, wrong perm key, bogus purge, tag-wrap belongs in Phase 3) → fresh MCP validator confirmed all findings against live DB → Agent 5 implemented corrected plan → tsc clean (0 new errors) + lint clean. **F7 Phase 2 COMPLETE — all 5 tasks shipped + pushed.**
- **F7 Phase 3 Task 11 — migration 116 cluster locks + unlock endpoint + runbook** (SHIPPED `7fef1ad`) — `schema/116_f7_cluster_locks_and_perms.sql` (235 lines, sha256 `8bb8f62e`) + `schema/117_rollback_116_f7_cluster_locks.sql` (53 lines, sha256 `9c357dd0`) + `web/src/app/api/admin/newsroom/clusters/[id]/unlock/route.ts` (112 lines, sha256 `0b746952`) + `Current Projects/F7-PHASE-3-RUNBOOK.md` (351 lines). Migration 116 STAGED awaiting owner apply. Adds `feed_clusters.locked_by/locked_at/last_generation_run_id/generation_state` + FKs via DO-block (NO ACTION on locked_by, SET NULL on last_gen_run) + partial index. `claim_cluster_lock(cluster_id, locked_by, ttl_sec=600)` + `release_cluster_lock(cluster_id, locked_by)` RPCs with explicit `p_locked_by` param (auth.uid() is NULL under service role — Agent 4 P0 catch) and ROW_COUNT diagnostics. Seeds perms `admin.pipeline.run_generate` + `admin.pipeline.release_cluster_lock` (mirrored from run_ingest role sets). Seeds rate_limits `newsroom_cluster_unlock` (10/60s) + settings `pipeline.default_category_id` (markets fallback). Unique partial indexes on `articles(cluster_id)` + `kid_articles(cluster_id)` WHERE cluster_id NOT NULL AND deleted_at IS NULL (belt+suspenders vs lock). RPCs granted to service_role only. Task 11 endpoint `POST /api/admin/newsroom/clusters/:id/unlock` — admin override via `release_cluster_lock(id, NULL)` + record_admin_action audit. Full F7 PM §3a four-agent flow: Agents 1+2 investigators (caught 3 P0s: perm missing, lock RPCs missing, state='generated' CHECK violation) → Agent 3 reviewer → Agent 4 adversary RED verdict (3 P0s + 10+ real bugs including `auth.uid()` NULL under service role, nested finally{} cascade, `articles.body` NOT NULL unaddressed, hallucinated `article_sources`/`article_timelines` table names, wrong ON DELETE semantic, ROW_COUNT false-positive on missing UUID) → Agent 3 ADDENDUM resolved every finding → fresh MCP validator spot-checked addendum → Agent 5 implemented with 9 MCP pre-flight verifications (bump_perms_global_version vs bump_user_perms_version, NOT NULL display_name/category on permissions, NOT NULL display_name/scope on rate_limits, settings.value_type='string' not 'text'). tsc clean + lint clean.
- **F7 Phase 3 Task 12 — GET /api/admin/pipeline/runs/:id observability** (SHIPPED `6fd552a`) — `web/src/app/api/admin/pipeline/runs/[id]/route.ts` (149 lines, sha256 `53e143e5c76eada845ce0bbeb76d9ccc378d736a87c48c823e107d42ed866fbc`). Read-only endpoint for admin observability pane. Single PostgREST join fetches `pipeline_runs` by id + joined `pipeline_costs` child rows. Computes totals server-side: cost_usd, latency_ms, tokens (input/output/cache-read/cache-creation), cache_hit_ratio, retry_count, failure_count, step_count. Returns `{ ok, run, steps (sorted by created_at), totals }`. Permission: `admin.pipeline.runs.detail` (verified live). UUID shape validation before DB hit (defensive). maxDuration=15. No mutations, no audit. FK `fk_pipeline_costs_pipeline_run_id` auto-detected by PostgREST. tsc clean (0 new errors) + lint clean.
- **F7 Phase 3 Task 13 — migration 118 persist_generated_article RPC + wrapper** (SHIPPED `8428fc7`) — `schema/118_f7_persist_generated_article.sql` (311 lines, sha256 `7d52060a`) + `schema/119_rollback_118_persist_generated_article.sql` (15 lines, sha256 `0688373f`) + `web/src/lib/pipeline/persist-article.ts` (164 lines, sha256 `4e6f8852`). Migration 118 STAGED awaiting owner apply (schema-independent of 116 — can apply in any order). `persist_generated_article(p_payload jsonb)` RETURNS TABLE(article_id, slug, audience) — SECURITY DEFINER, service_role GRANT only. Single-transaction insert of articles/kid_articles + sources/kid_sources + timelines/kid_timelines + quizzes/kid_quizzes with slug collision retry (3 attempts, hex-tail suffix), audience routing, empty-title fallback (`article-<run_id_prefix>`), WITH ORDINALITY → sort_order, quiz options[].is_correct stripped into metadata.correct_index. `articles.status='draft'` NOT 'published' — admin review flow promotes. `moderation_status='pending'` default. `published_at` NULL on generate. kid_quizzes.retention_policy defaults to `'delete_on_parent_request_or_12mo_inactive'` (omit). persist-article.ts wrapper: PersistArticlePayload + Source/Timeline/Quiz sub-types, PersistArticleError class with pg code + detail passthrough, RPC cast pattern (matches adminMutation.ts) — types:gen regenerates post-apply. MCP pre-flight verified all spec columns; found articles.summary does NOT exist (uses excerpt + metadata.summary). tsc clean (0 new errors) + lint clean.
- **F7 Phase 3 Task 10 — POST /api/admin/pipeline/generate (12-step chain)** (SHIPPED `b0ef4f0`) — SINGLE LARGEST TASK IN PROJECT. `web/src/app/api/admin/pipeline/generate/route.ts` (1737 lines, sha256 `80b742b3de5a8cca7763778ee161760344e758fd9dc70cb3a61822ec84abdbc5`) + `web/src/lib/pipeline/render-body.ts` (21 lines, sha256 `b73aa2e1`) + `web/src/lib/pipeline/logger.ts` (55 lines, sha256 `02dc7242`). Deps added: marked@^18.0.2, isomorphic-dompurify@^3.9.0, zod@^4.3.6. STAGED — requires migrations 116 + 118 applied + `npm run types:gen` before end-to-end testable. 12-step chain: `audience_safety_check → source_fetch → parallel(headline, summary, categorization) → body → source_grounding → plagiarism_check → timeline → [kid_url_sanitizer if kid] → quiz → quiz_verification → persist`. Perm `admin.pipeline.run_generate`. Audience-conditional kill switch (`ai.adult_generation_enabled` / `ai.kid_generation_enabled`). Daily cost cap pre-flight via `pipeline_today_cost_usd()` → 402. Rate limit `newsroom_generate` 20/3600s → 429. Cluster lock via `claim_cluster_lock(id, runId, 600)` → 409 contested. Prompt-injection hardening: `<source_article>` wrap + closing-tag escape on BOTH source bodies and freeform_instructions. Writer emits markdown `body`; `renderBodyHtml` converts to sanitized HTML (marked + isomorphic-dompurify). Category fallback chain: writer → cluster → settings.pipeline.default_category_id → schema_validation error. Prompt fingerprint: sha256 hex over `{step, system, user}` triples. AbortSignal plumbed through every callModel call. Discovery state machine: clustered → generating (at lock) → published (success) / ignored (audience_mismatch) / clustered (retryable fail). Finally block: 4 nested try/catches (state reset → unlock → pipeline_runs update → audit) — one failure doesn't cascade. HTTP status matrix: 402 cost_cap, 409 cluster_locked, 422 mixed_audience/scrape_empty, 429 rate_limit, 499 abort, 500 schema_validation/persist_conflict, 502 provider_error, 503 kill_switch, 504 timeout. Known deferred: plagiarism rewrite loop (scan ships; rewrite TODO), layer-1 per-category prompt overrides (not wired), pipeline_runs.error_type column (stashed in output_summary jsonb — schema gap for future migration). Full F7 PM §3a four-agent flow (largest scope to date): Agents 1+2 investigators → Agent 3 reviewer → Agent 4 adversary RED (3 P0s + 10+ bugs) → Agent 3 ADDENDUM → fresh MCP validator caught hallucinated table names → Agent 5 implemented with 8 MCP verifications. tsc clean + lint clean. **F7 Phase 3 Tasks 10-13 COMPLETE — all 4 tasks shipped + pushed. Tasks 14-19 next session.**
- **F7 Phase 4 Task 20 — Newsroom home page /admin/newsroom** (SHIPPED `6938c8c`) — `web/src/app/admin/newsroom/page.tsx` (474 lines, sha256 `708a5b2180808d3f0a30a0f5063a4d7350812f93db60e61231dd6cdc276b7f02`). NEW directory + file. NO migration, NO new perm (uses `ADMIN_ROLES` client gate matching settings page; `admin.pipeline.run_generate` checked at backend per existing routes). Single client-component file with `ToastProvider`-wrapped inner; auth pattern verbatim from settings page (supabase.auth.getUser → user_roles join → `ADMIN_ROLES.has(name)` → `router.push('/')` on fail). Data: `feed_clusters.select('id, title, summary, is_breaking, created_at, updated_at').eq(is_active, true).order(updated_at desc).range(offset, offset+19)` + try-select for `locked_by, locked_until` with `as unknown as {data, error}` cast applied **post-await on destructured result** (P1-2 from adversary; mid-chain cast would break the builder); column-not-found from migration 116 STAGED gracefully degrades — no lock UI rendered. Per-card render: BREAKING badge (red), Locked badge (warn) with title-tooltip via wrapping span (Badge has no title prop), title fallback "Untitled cluster", 200-char summary truncate with "(no summary)" muted fallback, relative time helper hand-rolled (date-fns NOT in package.json — verified), Generate adult / Generate kid buttons (both `disabled={locked || busyId===key}`, both POST `/api/admin/pipeline/generate {cluster_id, audience}`, on ok navigate to `/admin/pipeline/runs/:run_id` else toast with `(run <8-char-prefix>)` if json.run_id present per P2-1), Unlock button (only when `locked_by !== null`, POST `/api/admin/newsroom/clusters/:id/unlock`, refresh on ok), View link → `/admin/newsroom/clusters/:id` (Task 21 not built yet, renders 404 — acceptable scaffolding). Page header: Refresh feeds button (POST `/api/newsroom/ingest/run`, distinct 429 toast `'Refreshing too fast. Try again in a moment.'` per P1-3 — separate from generic 500 toast) + Pipeline runs nav. Pagination: simple offset-based with Load more button, 20 per page; `load(true)` resets `offset=0` before fetch (P1-1 — otherwise stale offset after Refresh yields empty range). Empty state via `EmptyState` with `description=` prop (NOT `body=` — adversary correction; verified `EmptyState.jsx:20`). Loading via `Spinner`; error via Toast + EmptyState. Imports pruned (`useCallback` dropped — unused; `useMemo` kept for supabase client per settings pattern). 7 STAGED dependencies documented in TSDoc header (migrations 116/118/120 + Tasks 21/22/27). Improved 3-agent flow: PM FACTS_task20.md via 3 MCP queries + 4 admin-page reads (caught `feed_clusters` has no audience column up front — design renders 2 inline buttons per card; existing `/admin/pipeline/page.tsx` shell coexists per fence) → 1 investigator (verified every component prop signature against .jsx source: Page+PageHeader exports, Button variants `primary|secondary|ghost|danger`, Badge variants, Toast variants, EmptyState `description=` prop correction, ADMIN_ROLES is frozen Set use `.has()`; flagged sequential vs parallel select tradeoff; documented all edge cases) → 1 adversary YELLOW (verified Task 11 unlock endpoint exists; verified Generate Zod accepts `{cluster_id,audience}` alone with other fields defaulted; ratified investigator's prop-name correction; caught P1-1 offset reset, P1-2 cast placement, P1-3 429 distinction; P2 run_id in failure toast, prune unused, date-fns check) → 1 implementer with fence; 5 deviations all justified (Link import added, `'use client'` directive placement, type narrowing via Pick<>, inline truncate helper, Badge tooltip via wrapping span). Self-verified: tsc exit 0, lint clean, no emoji, no `as any`/`@ts-expect-error`. **Did NOT click-through browser** — runtime/visual integrity needs owner eyeball verification on dev server. Phase 4 has higher UI verification bar; flagging this explicitly per CLAUDE.md "if you can't test the UI, say so explicitly." Net delta: +474 new file. 3 agent dispatches.
- **F7 Phase 4 Task 28 — cost tracker dashboard /admin/pipeline/costs** (SHIPPED `16822db`) — `web/src/app/admin/pipeline/costs/page.tsx` (594 lines). NEW directory + file. Single client component. Parallel load: `pipeline_today_cost_usd()` RPC (today's UTC-day spend, matches cost-tracker.ts cap-check path) + 3 settings rows (`pipeline.daily_cost_usd_cap` = $10, `pipeline.per_run_cost_usd_cap` = $0.50, `pipeline.daily_cost_soft_alert_pct` = 50 — MCP-verified live) + 30-day `pipeline_costs` slice `.range(0, 9999)` (scale note in TSDoc — fine for launch; replace with daily rollup view if this ever times out) + top-10 `pipeline_runs` ordered by `total_cost_usd desc` with `.not('total_cost_usd', 'is', null)` filter. Per-model 24h/7d/30d aggregates computed client-side from the single 30d fetch (no N queries). Five `PageSection`s: (1) today-vs-cap three-StatCard row + full-width progress bar coloured green <50% / yellow 50–80% / red >80% per spec literals (soft_alert_pct shown separately in settings block since its config value of 50 overlaps the yellow threshold), (2) per-model breakdown table sorted by 30d cost (model / 24h / 7d / 30d / tokens30d / calls30d) with `tabular-nums`, (3) 30-day CSS-only bar chart via flex + height% over UTC-day buckets matching the RPC's `date_trunc('day', now() AT TIME ZONE 'UTC')` — bars get `minHeight: 2px` for non-zero days so single-cent days remain visible, Y axis auto-scales to `max(day.usd)`, (4) top-10 outliers table with each row linking to `/admin/pipeline/runs/:id` (Task 27 target — may 404 until that ships), (5) read-only settings StatCards with "Edit in settings" aside-link to `/admin/settings` (Task 29 target). Hand-rolled helpers inline: `relativeTime`, `fmtUsd` (`.toFixed(4)`), `fmtCap` (`.toFixed(2)`), `utcDayKey` (slice ISO 0..10 — ISO is UTC-encoded so this aligns with the RPC), `thirtyDayKeys` (builds last 30 UTC day strings via `setUTCHours(0,0,0,0)` + `setUTCDate`). Empty states for zero-rows and zero-peak-day chart. Uses shared admin kit (`Page`, `PageHeader`, `PageSection`, `StatCard`, `Spinner`, `EmptyState`, `adminPalette` ADMIN_C/F/S). Top-level error banner surfaces any of the 4 parallel queries failing. No emoji, no `as any`, no `@ts-expect-error`, no new deps. No new API endpoint (spec called for direct supabase client + existing RPC). Investigator → adversary → implementer flow: investigator MCP-verified `pipeline_costs` 22 columns + 3 settings keys present + `pipeline_today_cost_usd()` source + `pipeline_runs.total_cost_usd` column; adversary flagged scale ceiling (`.range(0, 9999)` cap + note), UTC-day timezone alignment, empty-data paths, outlier table must link runs not cost rows (one `pipeline_runs` row per run — correct target), client-side 24h/7d/30d aggregation to avoid N queries. All absorbed. lint-staged prettier reformatted on commit (auto). Self-verified: `tsc --noEmit` exit 0 aside from pre-existing unrelated errors in `web/src/components/admin/GenerationModal.tsx` (Task 22 parallel work, untracked); grep of tsc output for "costs" returned zero hits. `next lint --file` clean. Only staged my new file (left parallel Task 22/27/29 untracked work alone). Did NOT browser-test — runtime/visual integrity needs owner dev-server click-through per Task 20 precedent on Phase 4 UI. Net: +594 new file. 1 commit touching 1 file.
- **F7 Phase 3 Task 19 — orphaned runs cleanup cron** (SHIPPED `9b9a32e`) — `web/src/app/api/cron/pipeline-cleanup/route.ts` (133 lines, sha256 `155593b85c85e42551c7576774cd70d9f3eeec0dc851a437461cda622a5dc927`) + `web/vercel.json` (+4 lines, 10th cron entry). NO migration, NO new perm (cron uses `verifyCronAuth` — x-vercel-cron header OR CRON_SECRET bearer; CRON_SECRET env already in use). Three idempotent best-effort sweeps fire every 5 min (`*/5 * * * *`): (1) orphan **runs** — `pipeline_runs.status='running'` AND `started_at < now()-10min` → `status='failed', error_type='abort', error_message='Orphaned run — auto-cleanup'`, duration_ms left NULL (per-row compute would need RPC; documented). Threshold 10min vs generate's `maxDuration=300s` = 2× safety margin. (2) **discovery items** — both `discovery_items` + `kid_discovery_items` rows in `state='generating'` AND `updated_at < now()-10min` → `state='clustered'` so next ingest can re-queue. **Adversary P1-A catch**: investigator + FACTS missed this sweep entirely; without it, items stay stuck in 'generating' forever when generate's lambda dies before its finally runs (generate's finally is the only other code that resets these). (3) orphan **locks** — `feed_clusters` rows with `locked_until < now()` → `locked_by/locked_at/generation_state` = NULL. Double-insurance against `release_cluster_lock` failures. All three sweeps wrapped in independent try/catch + console.error log on PostgREST error; response is ALWAYS 200 with per-sweep `errors.<sweep>: 'orphan_<x>_failed'` generic codes (P2-C from adversary — full error text only in server log; Vercel cron must not retry on 5xx). Per-sweep `count` from `.select('id')` after each UPDATE. `as never` cast on UPDATE payloads (Task 18 pattern; STAGED columns `error_type` + `locked_*` + `generation_state` not yet in generated types). `runtime='nodejs', dynamic='force-dynamic', maxDuration=15`. `withCronLog('pipeline-cleanup', run)` wraps both `GET` + `POST` exports (existing pattern from `freeze-grace`). vercel.json: appended 10th entry preserving 2-space outer / 4-space property indent. Owner already on Pro tier (9 existing crons exceeds hobby's 2-cron limit) so per-minute scheduling is supported. Depends on migrations 116 + 120 — until applied, sweeps fail silently and cron is no-op (acceptable while pipeline STAGED). Improved 3-agent flow: PM FACTS_task19.md via 3 MCP queries + cron pattern read from `freeze-grace/route.js` (caught Vercel plan-tier consideration + .ts vs .js for new files up front) → 1 investigator (structured claims; verified Supabase `.update().select('id')` returns array via grep of existing usage; simplified locks cast from full reshape to `as never`; flagged duration_ms tradeoff) → 1 adversary YELLOW (P1-A discovery items orphan gap — REAL miss by FACTS+investigator; P2-A flagged generate finally race for follow-up; P2-C error-text leak; P2-D json formatting) → 1 implementer with fence; 0 deviations. Self-verified: tsc exit 0, lint clean, no emoji, vercel.json JSON valid, third sweep present, generic error codes confirmed (raw error text only in console.error). Note: implementer reported "locked_* in generated types" — actually that line is `users.locked_until`, not feed_clusters. The `as never` cast hides the missing-column issue at compile time; runtime fail-and-log path is correct. Net delta: +133 new file, +4 vercel.json. 3 agent dispatches.

**F7 PHASE 3 COMPLETE — all 10 tasks (10-19) shipped.** Migrations 112 + 116 + 118 + 120 STAGED awaiting owner apply via Supabase SQL editor; then `npm run types:gen`, then route + cron go live.

- **F7 Phase 3 Task 18 — POST /api/admin/pipeline/runs/:id/cancel** (SHIPPED `31275c6`) — `web/src/app/api/admin/pipeline/runs/[id]/cancel/route.ts` (158 lines, sha256 `6605fdb1f77b9168a620f2c951f6e637f0c467097107786a7ca539758a44ce68`). NO migration, NO new perm (`admin.pipeline.runs.cancel` already seeded live — MCP verified no inheritance/auto-grant; owner/admin/editor only). SOFT cancel design (LOCKED): mark `pipeline_runs.status='failed', error_type='abort', error_message='Cancelled by admin'` with `.eq('status','running')` re-check to avoid stomping a finally that beat us → best-effort `release_cluster_lock(p_cluster_id, p_locked_by=run_id)` via generate's `service.rpc as unknown as` cast pattern (idempotent per migration 116 L120-121 — double-release safe no-op) → best-effort discovery items reset from `state='generating'` → `'clustered'` via NATIVE `.from(union).update().eq().eq()` (no cast — Supabase JS typed overloads support audience-routed union literal; investigator §D catch reducing code vs FACTS initial skeleton) → audit via `recordAdminAction({action:'pipeline_cancel', targetTable:'pipeline_runs', targetId:params.id, newValue:{cluster_id, audience, soft_cancel:true, was_status, was_started_at}})` (P2-B forensic context from adversary). output_summary writes `{cancelled_by_admin:true, error_type:'abort'}` not just `{error_type}` (P1-A from adversary — distinctive marker for log review so cancel-by-admin is distinguishable from any other abort). Gates: `pipeline_type!=='generate'` → 400, `status!=='running'` → 409, null cluster_id OR null audience → skip discovery reset. Worker mid-step abort is out of scope (would require status polling at 10+ generate callModel sites; separate task). Race asymmetry documented in TSDoc: cancel-after-finally is no-op (re-check filters 0 rows), cancel-before-finally gets overwritten by generate's non-status-gated finally — correct tradeoff since finally must complete bookkeeping regardless. Depends on migration 120 for `error_type` column — symmetric with generate's L1581 dep; acceptable while pipeline is STAGED. `maxDuration=15` (cancel is fast DB-only, no LLM). Improved 3-agent flow: PM FACTS_task18.md via 3 MCP queries (caught STAGED state of migration 116 RPCs + feed_clusters columns up front; documented SOFT vs HARD cancel tradeoff) → 1 investigator (structured claims; dropped unnecessary discovery-items cast via §D verification, tightened audience gate, flagged migration 120 hard-block with symmetry justification) → 1 adversary YELLOW (MCP-verified `release_cluster_lock` body for idempotency + `record_admin_action` for lack of action enum CHECK; caught P1-A output_summary stomp needing distinctive marker + P2-B was_status/was_started_at for audit forensics) → 1 implementer with fence; 0 deviations. Self-verified: tsc exit 0, lint clean, no emoji, no banned strings, single new file + new directory. Net delta: +158 new file. 3 agent dispatches.
- **F7 Phase 3 Task 17 — POST /api/admin/pipeline/runs/:id/retry** (SHIPPED `0361d16`) — `web/src/app/api/admin/pipeline/runs/[id]/retry/route.ts` (153 lines, sha256 `c080b003ccc1a41bf065212d0d975d62616c0591bbb196fe64881ed53ad053a5`). NO migration, NO new perm (`admin.pipeline.runs.retry` already seeded live per MCP). NO refactor of generate route (internal fetch coupling deliberate — scope-bounded). Pattern: perm gate via `requirePermission` + user client → UUID regex validate → service client SELECT failed run (cols: id, status, pipeline_type, cluster_id, audience, provider, model, freeform_instructions, output_summary) → gates: `pipeline_type!=='generate'` → 400, `status!=='failed'` → 409, missing cluster_id/audience → 422 → internal same-origin fetch to `/api/admin/pipeline/generate` with `cookie` header pass-through (generate re-auths with same session via `requirePermission` → `createClient` → Next.js `cookies()` chain) → body parse (swallow non-JSON) → audit via `recordAdminAction({action:'pipeline_retry', targetTable:'pipeline_runs', targetId:old_run_id, newValue:{new_run_id, cluster_id, audience, original_error_type}})` only on `response.ok && newRunId` → response spreads generate's body plus `old_run_id` + `new_run_id` at generate's status. `runtime='nodejs', dynamic='force-dynamic', maxDuration=300` (matches generate). Internal `fetch(...)` wrapped in try/catch (adversary P1 + investigator self-flag): dispatch failure → Sentry + 500 'Retry dispatch failed' instead of uncaught propagation. `originalErrorType` extracted from legacy `output_summary->>'error_type'` then `->>'final_error_type'` fallback (migration 120 promotes to first-class column — STAGED). Generate's `claim_cluster_lock` prevents double-click race; stale-lock scenarios wait 10min TTL OR Task 18 cancel OR Task 19 cron (documented). Improved 3-agent flow: PM FACTS_task17.md via 2 MCP queries + route pattern read from Task 12 sibling (caught handoff option A vs refactor tradeoff up front; documented internal-fetch as chosen coupling) → 1 investigator (structured claims; flagged missing fetch try/catch in FACTS skeleton) → 1 adversary YELLOW (ratified fetch try/catch P1 + added P1 for audit forensics via `original_error_type`; 3 P2s logged — SSRF-lite from Host header, stale-lock UX, `run_id`/`new_run_id` duplication doc) → 1 implementer with fence; 0 deviations (Prettier multi-line reformat only). Self-verified: tsc exit 0, lint clean, no emoji, no banned strings, single new file + new directory. Net delta: +153 new file. 3 agent dispatches.
- **F7 Phase 3 Task 16 — `pipeline_runs.error_type` column + backfill + route dual-write** (SHIPPED `7ed6b2c`) — `schema/120_f7_pipeline_runs_error_type.sql` (50 lines, sha256 `2f4bac1aa21fe6f3b495455143647cdef8e2a7173a113cb8d2e8a3b39cf768a6`) + `schema/121_rollback_120_f7_pipeline_runs_error_type.sql` (17 lines, sha256 `59c250257b4907e7822777d976e5b18a687bc144ae49eabf2eb3ee5ad5d11c93`) + `web/src/app/api/admin/pipeline/generate/route.ts` (1731 → 1734, +3 lines net). Migration STAGED — adds `error_type text NULL` column to `pipeline_runs` (idempotent via ADD COLUMN IF NOT EXISTS), COMMENT pointing at `classifyError()` + `statusForError()` (vocabulary owner), backfill from legacy `output_summary->>'error_type'` and `output_summary->>'final_error_type'` keys via triple-guarded UPDATE (covers both failRun and main-finally semantics). No CHECK constraint (vocabulary will extend in Phase 4). No index v1 (Task 12 obs is per-key lookup; Phase 4 dashboard may want partial index — flagged). Rollback drops column with IF EXISTS. Route edits dual-write the real column AND keep legacy `output_summary` stash for one cycle (Task 16 follow-up will remove): `failRun` helper at L1656-1668 adds `error_type: errorType` field to UPDATE payload, comment block replaced; main finally at L1559-1581 adds `error_type: finalErrorType` to UPDATE payload, comment block replaced, `final_error_type` key kept in outputSummary blob with inline comment. Both UPDATE payloads cast `as never` (lower-friction than `as unknown as Record<string,unknown>` per intra-pipeline-route precedent at L1483-1489 + Task 13's persist-article RPC pattern); cast removable post `npm run types:gen` — flagged follow-up. **Critical deploy ordering** (adversary YELLOW P0): supabase-js `.update()` resolves with `{ error }`, does NOT throw on PostgREST errors, so if route ships before migration applies the existing try/catch at L1582+L1668 NEVER FIRES — `pipeline_runs` rows would stick at `status='running'` with no log. Mitigation: owner applies migration 120 BEFORE Vercel deploy (acceptable since Task 10 generate endpoint itself is STAGED — no production traffic until owner cuts the gate). Improved 3-agent flow: PM-built FACTS_task16.md via 4 MCP queries (caught handoff drift on `pipeline_costs.error_type` confusion) → 1 investigator (structured claims; verified `as never` vs `as unknown as` codebase ratio 15:62, intra-pipeline-route precedent confirmed) → 1 adversary YELLOW (caught real P0 deploy-order/silent-no-op + P1 COMMENT vocab drift `permission_denied` already exists in statusForError but missing from enumerated comment → replaced enumeration with code pointer; P2 cast follow-up flagged) → 1 implementer with fence; 0 deviations. Self-verified: tsc exit 0, lint clean, no emoji on SQL files, only 2 UPDATE sites edited (3rd `from('pipeline_runs')` at L515 is the INSERT — confirmed unchanged), `final_error_type` key preserved in outputSummary. Net delta: 50 + 17 new SQL, +3 route. 3 agent dispatches.
- **F7 Phase 3 Task 15 — Layer 1 per-(cat,step,audience) prompt overrides** (SHIPPED `8acde50`) — `web/src/lib/pipeline/prompt-overrides.ts` (127 lines, sha256 `4f0b6a14fe6155eacd1b0632bee6277b2262db9488150516b9da91c881621bb0`) + `web/src/app/api/admin/pipeline/generate/route.ts` (1712 → 1729 lines, sha256 `f30b5d029a2063ce4c2a835435d112546fe72259eb5dc011769e9291bd7cb8a5`). NO migration (table `ai_prompt_overrides` already exists from migration 114 LIVE — handoff §5 was wrong about needing migration 121). NO new perms (service client read bypasses RLS; admin UI write perms ship with Phase 4). Helper exports `StepName` (12-name literal union mirroring DB CHECK), `PromptOverride`, `PromptOverrideMap = Map<StepName, string>` (narrowed key type prevents typo silently disabling override), `fetchPromptOverrides(supabase, clusterCatId, clusterSubcatId, audience)`, `composeSystemPrompt(base, override)`. Fetcher: isUuid-guards `clusterCategoryId` before interpolation into PostgREST `.or()` filter (adversary P1: PostgREST filter strings are NOT parameterized); single SELECT via `ai_prompt_overrides_lookup_idx` (~5ms); fail-OPEN on any error (warn log + empty Map) — Layer 1 is customization, not a safety gate. In-JS specificity score: `(cat_match?2:0)+(subcat_match?2:0)+(aud!='both'?1:0)`, per-step take MAX-scored rows only, concat with `\n\n`. composeSystemPrompt: base unchanged when no override; else `base.trimEnd() + "\n\nADDITIONAL INSTRUCTIONS (admin-set):\n" + override`. Route wiring: import added on L51; fetch + info-log inserted INSIDE `try{}` block after settings fetch, before L708 audience_safety_check call (adversary P1: log must be inside try); 10 of 10 callModel `system:` fields wrapped — audience_safety_check, headline, summary, categorization, body, source_grounding, timeline, kid_url_sanitizer, quiz, quiz_verification. promptParts debug ledger left unchanged (captures pre-override prompts for audit). Uses `clusterRow.category_id` not writer's `catParsed.category_id` (predictable admin surface; pre-categorization steps have access). Subcategory always `null` in v1 (Phase 4 will derive from cluster). Known deferred: plagiarism rewrite (11th LLM call in plagiarism-check.ts) does not receive overrides this cycle — breaking helper signature not worth it for rare path; `freeform_instructions` positioning (currently user turn, §3.4 specifies system) stays v1 as-is. Anthropic prompt cache busts on override edit — acceptable, documented. Improved 3-agent flow (same as Task 14): PM-built FACTS_task15.md via 6 MCP queries (caught 3 handoff drifts up front: table exists, no `layer` col, layer numbering wrong) → 1 investigator (structured claim table; caught FACTS §7.2 over-prescription on summary/body const extraction) → 1 adversary YELLOW (ratified investigator's catch; added 3 P1s: StepName union for typo safety, assertUuid for PostgREST injection, log placement inside try; 4 P2s folded into contract) → 1 implementer with fence; deviation logged (+17 vs ~+12 estimate, pretty-printed multi-line arg layout). Self-verified: tsc exit 0, lint clean both files, no emoji, no `as any`/`@ts-expect-error`, 10 composeSystemPrompt wraps confirmed via grep. Net delta: +127 new file, +17 route. 3 agent dispatches (same as Task 14).
- **F7 Phase 3 Task 14 — plagiarism rewrite loop port** (SHIPPED `af436cd`) — `web/src/lib/pipeline/plagiarism-check.ts` (120 lines, sha256 `9cf3ebff49e5d65dd757bc7751e23f2d3d127a420495572208186d422375bf46`) extracts inline `getNgrams` + `checkPlagiarism` from `web/src/app/api/admin/pipeline/generate/route.ts` (1727 lines, sha256 `0c28a2e61b7bc830d3896710d8fe02598dd04125e6c6602d9cde4c5db343136e`) and adds `rewriteForPlagiarism({body, sourceTexts, flaggedOutlets, model, pipeline_run_id, cluster_id, signal})` Haiku-backed rewrite helper. Trigger: `maxOverlap >= settings.plagiarism_rewrite_pct` (20). Helper guards: post-`cleanText` length≥100 + same-text reject → return original with `rewritten:false` (cost still accumulated since LLM call billed); rethrows `CostCapExceededError` + `AbortedError` (fail-closed invariant #3); swallows `ProviderAPIError` / `RetryExhaustedError` / `ModelNotSupportedError` / `ZodError` with `pipelineLog.warn`. Route plag step: re-checks with `checkPlagiarism` only when `rewriteRes.rewritten=true`; accepts rewritten body iff `secondCheck.maxOverlap < first.maxOverlap` (strict). Final WARN logged when `finalPlagOverlap > flag_pct` (25) with both percentages — does NOT fail run (snapshot parity; human review at Phase 4 Task 23 gates publish). FlaggedOutlets fallback: empty array → `'source articles'` literal in system prompt (avoids "TOO SIMILAR to source articles ()" embarrassment). Improved 4-agent flow: PM-built `FACTS_task14.md` ground-truth sheet via 6 MCP queries + file reads → 1 investigator (structured claim-table output, every row cited file:line or MCP query) → 1 adversary with fixed rubric (cost-cap, abort propagation, error-type routing, downstream mutation, idempotency, edge cases, FACTS drift, scope creep) returned YELLOW — caught real P0 (CostCapExceededError swallow violated fail-closed) + 3 P1s (flaggedOutlets empty case, missing post-cleanText length gate, FACTS §4 error_type strings drifted) + folded into addendum → 1 implementer with explicit MUST-NOT-TOUCH fence (classifyError, migrations, editorial-guide.ts, call-model.ts, errors.ts, clean-text.ts, logger.ts, imports reordering, all non-plag steps) shipped 1 minor deviation (`cluster_id ?? undefined` for LogShape strict typing). Self-verified: tsc exit 0 + lint clean (both files) + emoji grep empty + no orphan refs to deleted inline helpers + no `as any` / `@ts-expect-error` / leftover TODO. Net delta: +120 new file, -52 / +37 in route (net -15 lines). HAIKU_MODEL stays in route.ts (not exported from call-model.ts). 5 agent dispatches → 3 (saved 2 vs prior pattern) thanks to FACTS sheet + structured outputs eliminating reviewer + post-impl agent.

## Staged / pending apply (live state verified 2026-04-22 end-of-session via MCP)

**All F7 migrations applied to live `fyiwulqphgmoqullmrfn`:**
- `schema/112_kids_waitlist.sql` — `kids_waitlist` table LIVE
- `schema/116_f7_cluster_locks_and_perms.sql` — cluster-lock RPCs + perms + columns LIVE
- `schema/118_f7_persist_generated_article.sql` — `persist_generated_article` RPC LIVE
- `schema/120_f7_pipeline_runs_error_type.sql` — `pipeline_runs.error_type` column LIVE
- `schema/122_*.sql` — FK constraints (cluster_id across discovery_items + kid_discovery_items + pipeline_runs + pipeline_costs + kid_articles; ON DELETE symmetry) LIVE
- `schema/124_*.sql` — kids_summary drop / hygiene LIVE

`cd web && npm run types:gen` was run post-apply; `web/src/types/database.ts` now reflects all five new/altered surfaces. **Owner apply queue is empty.** (Dead-cast cleanup — Stream 3 D1-D11 — tracked separately in `MASTER_CLEANUP_PLAN.md`.)

## Blocked

- F7 Phase 1 Task 3 (migration 114 + full cost-tracker + settings seeds + ai_models catalog + kid tables + RLS hardening + articles_block_kid_jwt RESTRICTIVE policy) — blocked on §3i owner "apply" for live DB write. Drafting + stubbing unblocked.
- Push to origin/main — N commits ahead, awaiting §3f owner approval.
