export const metadata = {
  title: 'Category — Verity Post',
  description: 'Articles in this category on Verity Post.',
};

export default function CategoryLayout({ children }) {
  return children;
}
