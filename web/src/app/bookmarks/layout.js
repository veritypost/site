export const metadata = {
  title: 'Bookmarks — Verity Post',
  description: 'Your saved articles and discussions in one place.',
};

export default function BookmarksLayout({ children }) {
  return children;
}
