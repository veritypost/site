export const metadata = {
  title: 'Profile — Verity Post',
  description: 'Your Verity Post profile, activity, and settings.',
};

export default function ProfileLayout({ children }) {
  return children;
}
