node_modules
.next
src/types/database.ts
next-env.d.ts
*.tsbuildinfo
package-lock.json
public

# TEMPORARY: excluded from initial #20 autofix sweep so FIX_SESSION_1 #16
# (admin `as any` cleanup) lands on un-formatted ground for clean git blame.
# Remove this block after #16 ships, then run `npm run format` to back-fill.
src/app/admin
