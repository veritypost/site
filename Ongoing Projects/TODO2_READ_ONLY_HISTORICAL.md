# TODO — Owner

> **FROZEN — READ ONLY.** This file is historical reference as of 2026-04-27. The 10 session files at `Ongoing Projects/Sessions/Session_NN_*.md` are the canonical source of truth. Do not edit this file. Do not cite this file as authoritative. If a fact here conflicts with a session file, the session file wins.

Items that need owner involvement (even 5%) — a question, a SQL apply, a dashboard toggle, a product decision, a paid-tool sign-up, anything that touches owner judgment or owner-only access.

Each entry is **freshly verified against current code** at write time.

---

## CRITICAL — production is currently broken (live MCP-verified 2026-04-27)

## T0.1 — Family plan signup is dead in production

**Verified:** 2026-04-27 via live `SELECT FROM public.plans WHERE name IN (...)`.
**Owner action needed:** Run two UPDATE statements in Supabase SQL editor.
**Current state in code/DB:**
- `verity_family_annual` → `is_active=false, is_visible=false`
- `verity_family_monthly` → `is_active=true, is_visible=false`
- `/api/plans` filters on both flags. Family plan card is invisible on the pricing page; checkout for either Family variant is impossible.
- Cause: the plan-rewrite migration used `INSERT … WHERE NOT EXISTS`; pre-existing rows from a prior 04-23 migration kept the wrong flags.

**Fix (paste into Supabase SQL editor):**
```sql
UPDATE plans SET is_visible=true WHERE name='verity_family_monthly';
UPDATE plans SET is_active=true, is_visible=true WHERE name='verity_family_annual';
```
**Verify:** Re-query, confirm both rows show `is_active=true, is_visible=true`. GET `/api/plans` returns both. Pricing page renders the Family card.
**Risk:** None — pure UPDATE on 2 rows.

## T0.2 — `post_comment` RPC throws on every threaded reply + every paid-tier mention

**Verified:** 2026-04-27 via live `pg_proc` body query.
**Owner action needed:** Apply migration via Supabase SQL editor.
**Current state in code/DB:** Live `post_comment` body uses `blocks` table at two locations (reply-notification branch + mention-notification loop). The actual table is `blocked_users`. Every threaded reply will throw `relation "blocks" does not exist` and roll back. Every paid-tier mention notification same.
**What's missing:** Migration that does `CREATE OR REPLACE FUNCTION public.post_comment(...)` with `blocks` → `blocked_users` (two occurrences). Same function body otherwise; preserve T26's notification logic intact.
**Fix:** Draft migration at `Ongoing Projects/migrations/<date>_T0_2_post_comment_blocks_rename.sql`. Owner applies. Smoke test: post a threaded reply → verify reply lands AND a `notifications` row exists for the parent author.
**Live billing risk:** None — comment surface, no Stripe.

## T0.3 — `claim_push_batch` RPC missing → push cron is dead

**Verified:** 2026-04-27 against `web/src/app/api/cron/send-push/route.js:73` which calls `service.rpc('claim_push_batch', {p_limit: 200})`. Function does not exist in `pg_proc`.
**Owner action needed:** Sign off on the RPC shape + apply migration.
**Current state in code/DB:** Every push cron tick errors out and exits. Breaking news fan-out is dead. Reply notifications never deliver via push.
**What's missing:** RPC body. Should `SELECT … FOR UPDATE SKIP LOCKED LIMIT p_limit` from a pending-push queue table, mark claimed, return rows. Read the cron route consumer to derive exact row shape (fields the cron expects).
**Fix:** Draft migration `Ongoing Projects/migrations/<date>_T0_3_claim_push_batch_rpc.sql`. Owner reviews row-shape + applies. Smoke: insert a pending-push row, trigger cron, verify a `push_receipts` row + claim_push_batch returned the row.
**Live billing risk:** None.

## T0.5 — `current_kid_profile_id()` reads wrong JWT key — kids get degraded content

**Verified:** 2026-04-27 via live `pg_proc` body query.
**Owner action needed:** Apply one-line migration.
**Current state in code/DB:** Live function body: `SELECT NULLIF(auth.jwt() -> 'app_metadata' ->> 'kid_profile_id', '')::uuid`. Kid JWTs at `web/src/app/api/kids/pair/route.js:153` + `web/src/app/api/kids/refresh/route.js:114` put `kid_profile_id` at the **top level**, not in `app_metadata`. Function returns NULL for every kid JWT. `kid_visible_bands(NULL)` returns `{}`. Article RLS predicate degrades to `age_band IS NULL`. Phase 1 backfilled all kid-safe articles to `age_band='tweens'`, so kids see fewer articles than intended.
**Failure mode is fail-CLOSED** (kids see less, not adult content). Product-functional bug, not a security leak.
**What's missing:** Migration that rewrites the function to read top-level: `SELECT NULLIF(auth.jwt() ->> 'kid_profile_id', '')::uuid`. Consistent with `is_kid_delegated()` and `parent_user_id` reads.
**Fix:** Draft `Ongoing Projects/migrations/<date>_T0_5_current_kid_profile_id_top_level.sql`. Owner applies. Smoke: as a paired kid, SELECT `articles` filtered to a kid-safe cluster — should return rows where `age_band='kids'`.
**Live billing risk:** None.
**Blocks:** T3.1 (dual-band kid generation in TODO-AUTONOMOUS) — without this, the dual-band articles aren't visible to kids.

---

## CRITICAL — blocks AUTH-MIGRATION

## T345 — Beta-cron + AUTH-MIGRATION sequencing pre-flight

**Verified:** 2026-04-27 against `web/src/app/api/cron/sweep-beta/route.js` + `sweep_beta_expirations` RPC reference.
**Owner action needed:** Two pre-flight steps before AUTH-MIGRATION's first traffic ships.
**Current state in code:** Cron calls `sweep_beta_expirations` RPC. When `settings.beta_active='false'`, the RPC stamps `verify_locked_at=now()` for **every** beta user with `email_verified=false` — including owner-link recipients who never verified. Owner separately confirmed Supabase project setting "Confirm email" is currently **OFF**.
**What's missing:** Two pre-flights:
1. Flip Supabase Dashboard → Authentication → Sign In/Up → "Confirm email" to **ON**. Without this, `signInWithOtp` will issue sessions immediately on signup instead of waiting for the click.
2. Reconcile the legacy beta cohort (currently password + `email_verified=false`) before the next sweep run after migration. Three options: (A) bulk magic-link emails to legacy beta users, (B) bulk admin-confirm via the new `/admin/auth-recovery/` page, (C) keep `settings.beta_active='true'` through the migration window.
**Fix:** Owner picks (A/B/C) for cohort reconciliation, owner flips the Confirm-email setting. Then AUTH-MIGRATION's first traffic ships.

---

## T347 — Consolidate 8 user-state flags into one enum — schema work

**Verified:** 2026-04-27 against `Ongoing Projects/migrations/2026-04-27_T347_user_state_enum_DRAFT.sql` (file exists, marked DRAFT — DO NOT APPLY WITHOUT OWNER REVIEW).
**Owner action needed:** Review the draft migration shape, sign off on transition rules, schedule the cross-codebase caller sweep that lands with the migration.
**Current state in code:** 8 independent boolean/timestamp columns: `is_banned`, `locked_until`, `is_muted`, `muted_until`, `deletion_scheduled_for`, `frozen_at`, `plan_grace_period_ends_at`, `verify_locked_at`, `comped_until`. Nothing enforces sane combinations — a user can be in any combination. AccountStateBanner picks the highest-priority one; no DB-level constraint.
**What's missing:** A canonical enum + transition rules + check constraints + caller refactor.
**Fix:** Once owner approves: apply the migration, sweep all read paths (banner, RLS, RPCs, admin tools, analytics dashboards), drop the legacy columns last. T5 schema item — never applied autonomously.

---

## HIGH — superseded-by AUTH-MIGRATION but still on the path

## T320 — Owner-link Pro recipients are gutted (cohort=beta, email_verified=false)

**Verified:** 2026-04-27 against `web/src/app/welcome/page.tsx:106` + grep for `requires_verified` in `web/src/app/profile/settings/page.tsx`.
**Owner action needed:** Confirm AUTH-MIGRATION direction: "everyone verifies the same way" — i.e., drop the `isBetaOwnerLinkSignup` bypass and route everyone through magic-link signup. (System map §17 Phase 1 lock.)
**Current state in code:** `isBetaOwnerLinkSignup = cohort === 'beta' && !!plan_id` lets users past the welcome carousel only. Every other surface still hard-blocks the 21 `requires_verified=true` perms — they can't comment, follow, vote, bookmark unlimited, see own activity, or use TTS.
**What's missing:** Either (A) drop the bypass and onboard owner-link users via magic-link (auto-resolves under AUTH-MIGRATION), OR (B) carve out the requires_verified gate for cohort=beta users.
**Fix:** Owner picks. (A) is the cleaner future and aligns with the locked direction; (B) is an interim patch.

## T321 — Owner-link recipients locked out of `/profile/settings#billing`

**Verified:** 2026-04-27 against `web/src/app/profile/settings/page.tsx:80, 375`.
**Owner action needed:** Same call as T320 — locks together.
**Current state in code:** `SECTION_BILLING_VIEW: 'billing.view.plan'` — perm requires verified email. No cohort-beta bypass on billing surface. Owner-link Pro users can't reach the upgrade page even if they want to actually pay (when their comp window ends).
**Fix:** Resolves with AUTH-MIGRATION. Pre-migration mitigation: carve `billing.view.plan` out of `requires_verified` for cohort-beta-Pro users.

## T22 — iOS pick-username step under magic-link

**Verified:** 2026-04-27 against `VerityPost/VerityPost/AuthViewModel.swift:612-679` (current OAuth fallback path) + 389 (`handleDeepLink` calls `setSession()` directly).
**Owner action needed:** Confirm direction: under magic-link, the pick-username step happens post-signin (not in OAuth callback, since OAuth is being removed). Current code has no pick-username UI in the magic-link path.
**Current state in code:** OAuth (SignInWithApple, Google) uses web fallback; deep-link callback lands in `handleDeepLink` and skips username pick.
**What's missing:** Magic-link callback should detect first-time signup (no username yet) and route to a single-screen pick-username view before landing the user on Home.
**Fix:** Bundle into AUTH-MIGRATION execution session. Add `username IS NULL` check in `handleDeepLink` post-`setSession()`, route to `PickUsernameView`, route to Home only after username is saved.

## T23 — iOS sign-in skips server lockout/audit/daily-login bookkeeping

**Verified:** 2026-04-27 against `VerityPost/VerityPost/AuthViewModel.swift:165-188`.
**Owner action needed:** Decide if iOS auth contract should mirror web's hardened endpoints (`/api/auth/login-precheck` → `/api/auth/login-failed` → `/api/auth/login`) or stay with direct Supabase SDK signin + best-effort `last_login_at`. Under magic-link, the lockout shape changes (no password attempts to throttle; the equivalent is per-email-address magic-link send rate). Decision affects whether the iOS audit log captures sign-in events at all.
**Current state in code:** iOS calls `client.auth.signIn(...)` directly, then best-effort `update_own_profile` RPC for `last_login_at`. Web has the multi-step server-pass.
**Fix:** Bundle into AUTH-MIGRATION. Web's hardened path may simplify under magic-link too — coordinate web + iOS contract together.

## T200 — Signup username retry loop

**Verified:** 2026-04-27 against `VerityPost/VerityPost/AuthViewModel.swift:312-331`.
**Owner action needed:** Bundle into AUTH-MIGRATION; signup flow reshapes under magic-link.
**Current state in code:** 3-attempt retry with 300ms × attempt backoff for username trigger errors (P0002).
**Fix:** Reshape with AUTH-MIGRATION; magic-link signup decouples auth from username.

## T252 — Username availability race vs `auth.signUp`

**Verified:** 2026-04-27 against `VerityPost/VerityPost/AuthViewModel.swift:249-278`.
**Owner action needed:** Bundle into AUTH-MIGRATION.
**Current state in code:** `/api/auth/check-username` then `client.auth.signUp()` immediately after. Another device can claim the username between check and signup — auth row gets created with NULL username.
**Fix:** Move username validation inside the post-signin pick-username step (T22). Server-side upsert with uniqueness constraint handles the race naturally.

---

## HIGH — product / behavior decisions only owner can make

## T19 — Home feed preferences are decorative

**Verified:** 2026-04-27 against `web/src/app/profile/settings/page.tsx:2587-2600` (writes `users.metadata.feed.*` flags) + `web/src/app/page.tsx` (zero reads).
**Owner action needed:** Pick: (A) keep editorial hero + bias supporting slots based on category preferences + `minScore` / `kidSafe` filters; (B) remove the settings cards entirely if the product is intentionally editorial-only.
**Current state in code:** Settings page has a fully wired set of toggles for `cats`, `kidSafe`, `hideLowCred`, `showBreaking`, `showTrending`, `showRecommended`, `minScore`, display mode. Home page never queries these flags. Save-success toast lies to the user.
**Fix:** (A) requires server-side feed bias logic + home page reads. (B) is a deletion PR — same week. Recommendation: ship (B) unless personalization is on the near roadmap.

## T308 — Admin manual-sync downgrade ignores `frozen_at`

**Verified:** 2026-04-27 against `web/src/app/api/admin/subscriptions/[id]/manual-sync/route.js:100-150`.
**Owner action needed:** Clarify: when admin manually downgrades a frozen user to free, should `frozen_at` clear (since there's no plan to be frozen against), or persist (frozen state was set for a reason — disputed payment, etc.)?
**Current state in code:** Downgrade branch clears `plan_grace_period_ends_at` only. `frozen_at` is left untouched — comment in the file says "we leave verity_score / frozen_at alone." Frozen+free is logically incoherent.
**Fix:** (A) Clear `frozen_at` on admin-driven downgrade — clean exit. (B) Surface a confirmation modal ("This user is frozen — clear freeze too?") and let the admin decide per case. (C) Leave frozen+free state intact and document it as legitimate (e.g., for a future audit-trail purpose). Owner picks.

## T346 — Freeze scope: monetization-only or content lockout?

**Verified:** 2026-04-27 against `web/src/app/leaderboard/page.tsx` (3 `frozen_at IS NULL` filters) + grep across `web/src/app/api/comments/`, `web/src/app/api/votes/`, `web/src/app/api/follows/` (no freeze checks in those routes).
**Owner action needed:** Pick: is freeze meant to disable score scoring + DM + leaderboard ONLY (monetization signal), or also lock comments + votes + follows + reading (content lockout)?
**Current state in code:** Freeze gates scoring, DM, leaderboard visibility. Reading + commenting + voting + following are unaffected. Frozen user (e.g., disputed payment) can still post and follow.
**Fix:** (A) Monetization-only — document the intent so future agents don't "fix" it as a bug. (B) Content lockout — add `frozen_at IS NULL` to comment INSERT RLS, vote routes, follow routes. Owner picks.

## T34 — Downvotes are decorative — DEFERRED scoring decision

**Verified:** 2026-04-27 against `web/src/components/CommentThread.tsx:134` (sort: `upvote_count DESC` only).
**Owner action needed:** Pick a scoring algorithm: (A) Wilson score (binomial confidence interval) — most robust under low volume. (B) Simple subtraction `upvote_count - downvote_count * 0.5` — quick to ship, fine for low-traffic threads. (C) Leave downvotes decorative — drop the column from the comment fetch and the UI button to reflect actual behavior.
**Current state in code:** Both surfaces sort by upvote-only. `downvote_count` is fetched and rendered but never feeds sort.
**Fix:** Pick (A/B/C) and implement the chosen path.

## T35 — No rank-change notifications — DEFERRED ship/no-ship

**Verified:** 2026-04-27 against `web/vercel.json` (no rank-related cron) + grep across `web/src/app/api/cron/` (no rank diff cron exists).
**Owner action needed:** Decide: ship rank-change notifications (in-app only, weekly) or drop the idea?
**Current state in code:** Leaderboard exists; no rank diff job; no notification surface for rank changes.
**Fix:** If ship: weekly cron diffs each user's rank vs 7 days ago, in-app notification only (no push) for moves of 3+ spots, top-10 entry/exit, cap 1/week. If drop: close item permanently.

## T49 — iOS Username editable, web says immutable — contract mismatch

**Verified:** 2026-04-27 against `VerityPost/VerityPost/SettingsView.swift:1290-1294` (iOS editable) + `web/src/app/profile/settings/page.tsx:1716-1720` (web copy: "Usernames cannot be changed.").
**Owner action needed:** Pick the contract: (A) immutable — disable iOS field, document at signup; (B) changeable with rate limit (e.g., once per 90 days); (C) changeable with audit + ban-evasion check.
**Current state in code:** iOS sends a PATCH to `update_own_profile` RPC accepting username changes. Web tells users they can't.
**Fix:** Owner picks; align both surfaces.

## T365 — Pro pride pill in CommentRow

**Verified:** 2026-04-27 against `web/src/components/CommentThread.tsx:154` (query selects `id, username, avatar_url, avatar_color, is_verified_public_figure, is_expert` — no `plan_id`) + `web/src/components/CommentRow.tsx:215-218` (renders username + verified + expert; no Pro pill).
**Owner action needed:** Decide: is `plan_id` (or a derived `tier` flag) public? Is the Pro pill visible in comments to all readers?
**Current state in code:** Profile hero shows the Pro badge (T316 shipped). Comments do not — `plan_id` is not in the public_profiles_v whitelist.
**Fix:** If shipped: add a derived `is_pro` boolean to `public_profiles_v` (true when `plans.tier IN ('pro','family','family_xl')`), extend CommentThread fetch + CommentRow render. Owner decision is whether plan visibility crosses into the public surface or stays profile-only.

---

## HIGH — schema / migration apply

## T55 — Drop `ai_prompt_preset_versions` orphan table

**Verified:** 2026-04-27 against `Ongoing Projects/migrations/2026-04-27_T55_drop_ai_prompt_preset_versions.sql` (drafted; pre-flight DO-block refuses if rows present) + `web/src/types/database.ts` (table type still in schema).
**Owner action needed:** Apply the migration via Supabase SQL editor. Pre-flight guard makes it safe (refuses to drop if any rows exist).
**Current state in code:** Migration file exists. Table is orphan — pipeline_run.input_params.prompt_snapshot (T242) covers audit/replay needs.
**Fix:** Owner pastes the migration into Supabase SQL editor. After apply, regenerate `database.ts` types. Done.

## T2.7 — `billing_change_plan` + `billing_resubscribe` not idempotent — HIGH

**Verified:** 2026-04-27 via live `pg_proc` body queries.
**Owner action needed:** Apply migration + replay a Stripe webhook event manually to confirm no duplicate rows.
**Live billing risk:** **HIGH — Stripe webhook re-runs hit this.** Stripe's stuck-300s reclaim window can re-deliver the same event. Today: `billing_change_plan` appends a duplicate `subscription_events` row + re-runs `convert_kid_trial(p_user_id)` on the second hit; `billing_resubscribe` INSERTs a brand-new `subscriptions` row (RETURNING into `v_new_sub_id`) so a user ends up with TWO active subs. `users.plan_id` may flap.
**What's missing:**
- `billing_change_plan` — early-return / no-op if `users.plan_id` already matches `p_new_plan_id` AND the most recent `subscription_events` row matches the change.
- `billing_resubscribe` — early-return if the user already has an `active` `subscriptions` row matching the inputs.
**Fix:** Draft migration `Ongoing Projects/migrations/<date>_T2_7_billing_idempotency.sql` rewriting both functions with idempotent guards. Owner applies. Then replay the same Stripe webhook event twice in test mode (or against a recoverable real account); verify no duplicate `subscriptions` or `subscription_events` rows. Pair-review the migration before apply.
**Why this isn't split into an AUTONOMOUS code-half (unlike T0.4 / T2.1):** The fix is pure RPC body change inside an owner-applied migration. There's no separate "code ships → flag flips after test" step — owner applies the migration AND runs the replay test in the same session. Splitting would just create a no-op AUTONOMOUS entry. Pattern stays consistent: code-and-test live together when the code IS the SQL.

## T2.2 — `anonymize_user` doesn't redact comment + message bodies — HIGH (GDPR)

**Verified:** 2026-04-27 — needs live `pg_proc` confirmation, but REMEDIATION audit cited the function's flip-only behavior.
**Owner action needed:** Apply migration extending `anonymize_user`.
**Current state in code/DB:** Function flips usernames / sessions / auth_providers but leaves `comments.body`, `comments.body_html`, `comments.mentions`, `messages.body` verbatim. GDPR Art. 17 erasure exposure — soft-deleted user's content stays readable.
**Fix:** Migration extends `anonymize_user` to `UPDATE comments SET body='[deleted]', body_html=null, mentions='[]'::jsonb WHERE user_id=p_user_id` + same for `messages`. Wrap in transaction. Backfill any users already in `deleted` state. Test on a soft-deleted test user with comments + messages; confirm content redacted.

## T2.3 — Comment block enforcement is client-side only — HIGH

**Verified:** 2026-04-27 against `web/src/components/CommentThread.tsx:191-200` (web-side filter) + REMEDIATION's MCP `pg_policy` query.
**Owner action needed:** Apply RLS migration.
**Current state in code/DB:** RLS `comments_select` qual: `(status='visible' AND deleted_at IS NULL) OR user_id=auth.uid() OR is_mod_or_above()`. No `blocked_users` filter at the policy level. iOS and direct PostgREST/realtime clients see blocked users' comments through.
**Fix:** Migration adds `AND NOT EXISTS(SELECT 1 FROM blocked_users b WHERE b.blocker_id = auth.uid() AND b.blocked_id = comments.user_id)` to the policy. Postgres realtime evaluates RLS — propagation is automatic. Test on web, iOS, raw PostgREST: User A blocks User B → A's comment query returns no B comments.

## T3.8 — `resolve_report` doesn't notify reporter — MEDIUM

**Verified:** 2026-04-27 — RPC writes audit_log only.
**Owner action needed:** Apply migration.
**Current state in code/DB:** Reporter never sees outcome of their report. Moderation chain dead at the final link.
**Fix:** Migration adds `INSERT INTO notifications (user_id, type, title, body, action_url, metadata, email_sent)` for the reporter inside `resolve_report` RPC, with the resolution outcome. Smoke: report a comment → admin resolves → reporter sees a notification.

---

## HIGH — Stripe LIVE: paired tests (code half lives in TODO-AUTONOMOUS)

These three items have a code-half that's ready to ship behind a feature flag. The owner-test step is what's blocking the flag-flip. Stripe is live in production — no sandbox. Test on a known-recoverable real card (refund flow ready).

## T0.4 paired-test — `add-kid-with-seat` rollback path

**Verified:** 2026-04-27 against `web/src/app/api/family/add-kid-with-seat/route.ts:410-415, 466`.
**Owner action needed:** Once the AUTONOMOUS code-half lands behind a flag, manually trigger the failure path on a real test family account (force `kid_profiles.insert` failure via a DB constraint violation in staging). Verify the new Stripe line item is removed and the idempotency row state allows clean retry.
**Live billing risk:** **HIGH — billing-side mistake here is worse than the bug.** Pair-review before flag-flip.
**Code half (in TODO-AUTONOMOUS T0.4):** Track which Stripe op fired (`add` vs `patch`); on `add` rollback, call `removeSubscriptionItem(seatItem.id)` instead of restoring quantity; on terminal failure, clear/mark the idempotency row for clean retry.

## T2.1 paired-test — Cross-platform double-subscription guard

**Verified:** 2026-04-27 against `web/src/app/api/ios/subscriptions/sync/route.js` + Stripe webhook handler.
**Owner action needed:** Once the AUTONOMOUS code-half lands behind a flag, test the conflict UX: buy on Stripe → attempt iOS purchase → block with friendly error. Reverse direction.
**Locked decision (2026-04-27):** Block the second platform with `"You already have an active Verity Post subscription on [Stripe/Apple]. Manage or cancel it there before subscribing here."`
**Live billing risk:** **HIGH — wrong UX leaves customers stuck.**
**Code half (in TODO-AUTONOMOUS T2.1):** Partial-unique index `CREATE UNIQUE INDEX ON subscriptions (user_id) WHERE status='active'`. Both sync paths refuse the new sub if an active one exists on the other platform; structured 409 with platform-conflict reason.

## T2.7 paired-test — Billing RPC idempotency

(Listed above as the migration item — owner runs the replay test as part of the apply.)

---

## OPEN — owner decision before bundle ships

## T3.2 decision — Bundle Phase 5.6 graduation deep link with iOS parent UI?

**Verified:** 2026-04-27 — `VerityPost/VerityPost/FamilyViews.swift` (1,422 lines) has no advance/graduate UI. Server-side `advance-band/route.ts` + `graduate-kid` routes already shipped.
**Owner action needed:** Decide bundling.
**Options:**
- **(A) Ship Phase 5.5 standalone** (BandPanel + AdvanceToTweensSheet + GraduateKidSheet + GraduationClaimView in `FamilyViews.swift` — 7.5h). Graduation claim URL opens Safari fallback. All-iOS family loop is broken if parent + kid are both on iOS.
- **(B) Bundle 5.5 + 5.6 deep-link** (10-11h). Requires Universal Links setup (T3.4 in TODO-PRE-LAUNCH) — Apple Dev console step. Recommended; without it, the all-iOS family loop is half-broken.
**Recommendation:** (B) — half-broken iOS-first family flow is worse than waiting a session.

---

## T14 RPC half — `use_streak_freeze` RPC missing

**Verified:** 2026-04-27 against `web/src/app/profile/page.tsx:833-834` (streak renders as `${user.streak_current ?? 0}d` plain) + grep for `use_streak_freeze` (zero hits — only `use_kid_streak_freeze` exists).
**Owner action needed:** Approve the RPC + its draft migration. T5 schema item — halt-and-queue per runbook.
**Current state in code:** Adult plumbing exists in iOS (`streak_freeze_remaining` decoded in Models.swift; admin `streak_freeze` flag on). The freeze-restore action has no server endpoint. UI half can ship without the RPC (the "Streak reset — start a new one today" branch is RPC-free) but the "Use a freeze to restore" branch needs the RPC.
**Fix:** Draft `use_streak_freeze(p_user_id uuid)` SECURITY DEFINER RPC: validates `streak_freeze_remaining > 0`, restores `streak_current = streak_best`, decrements `streak_freeze_remaining`, logs to audit, returns `{success: true, remaining: int}`. Mirror what `use_kid_streak_freeze` does. Apply migration, then UI half (TODO-AUTONOMOUS T14 UI half) wires the button.

---

## HIGH — multi-week cross-surface coordination

## T357 — Web profile redesign cutover (delete legacy + move `/redesign/*` → `/profile/*`)

**Verified:** 2026-04-27 against:
- `web/src/app/profile/page.tsx` (legacy: 1,898 lines)
- `web/src/app/profile/settings/page.tsx` (legacy: 5,187 lines)
- `web/src/app/redesign/` (45+ files)
- `web/src/middleware.js:159-449` (`:3333` host-check / `_isRedesignPort` rewrite block intact)
- `web/package.json` (`dev:3333` script intact)

**Owner action needed:** Pick the cutover window. This is a single coordinated PR — once merged, redesign is live; pre-merge, both surfaces coexist on `:3000` and `:3333`.
**Current state in code:** Redesign tree is parallel to legacy. Redesign mounts at `:3333` via dev port logic in `middleware.js`. Legacy is what `:3000` serves.
**What's missing:** The 7-step cutover PR.
**Fix:**
1. Delete `web/src/app/profile/page.tsx` + `web/src/app/profile/settings/page.tsx` + 12 redirect-shim subpages.
2. Move 45 redesign files from `web/src/app/redesign/profile/` to `web/src/app/profile/`.
3. Drop dev-only artifacts: `_lib/demoUser.ts`, `isPreviewHost()` calls, `preview` prop plumbing, `redesign/preview/page.tsx`, `redesign/u/`.
4. Drop `:3333` middleware logic in `web/src/middleware.js` (`_isRedesignPort`, host-check, rewrite block, ALLOWED_ORIGINS entry).
5. Drop `dev:3333` from `web/package.json`.
6. Co-ship: T334 caller-side migration in PrivacyCard.tsx (replace two-statement client flow with `lockdown_self()` RPC call — RPC migration already drafted at `Ongoing Projects/migrations/2026-04-27_T334_lockdown_self_rpc.sql`).
7. Decide whether to flip `PUBLIC_PROFILE_ENABLED` (separate decision — T363 must ship first).

**Estimated:** ~7,200 line deletions + ~45 file moves + ~15 min import-fixup pass. T4 review (cross-surface).

## T358 — iOS adult profile redesign port

**Verified:** 2026-04-27 against `VerityPost/VerityPost/` — no `ProfileShell.swift`, `Palette.swift`, `AccountStateBanner.swift`, `AvatarEditorView.swift` files exist.
**Owner action needed:** Schedule the multi-week build window. Blocked by T357 (web cutover stabilization) + T360 (CategoriesSection + MilestonesSection on web).
**Current state in code:** Existing `ProfileView.swift` + `SettingsView.swift` + 18 supporting Swift files (~8-9k LoC) form the legacy iOS profile. None of the redesign Swift counterparts exist yet.
**Fix:** Build to mirror the web redesign's master/detail shell + 22-section model:
- `ProfileShell.swift` — `NavigationSplitView` on iPad + drawer-sidebar on iPhone, grouped section list (Library / Family & Expert / Settings / Account).
- `Palette.swift` — port `_lib/palette.ts` tokens (`Color.vpInk` / `vpInkSoft` / `vpInkMuted` / `vpAccent` / `vpDanger`, `VPSpace` enum, `VPRadius` enum). Type rules: serif system font for hero, system sans for body.
- `AccountStateBanner.swift` — port the 14-state tagged union.
- `AvatarEditorView.swift` — `LazyVGrid` of 72 swatches + neutrals row + native `ColorPicker` for hex/wheel + multi-size live preview.
- 16 section views per the web→iOS mapping table — some wrappers around existing Swift views (Identity, Sessions, Notifications, Plan, Bookmarks, Messages); some new builds (Privacy lockdown, Blocked, Data export+delete, Categories, Milestones, Expert profile editor + apply form).

Data layer (`PermissionService`, `AuthViewModel`, `StoreManager`, supabase client, REST hits) **stays unchanged**. No tier color — tier renders as plain text in `vpInkMuted`.

**Pre-flight:** T359 (`profile_visibility='hidden'` audit on iOS — parallel to T330's web fix). Verify no iOS surface leaks hidden-visibility users.

## T363 — Public profile redesign placeholder needs full rebuild

**Verified:** 2026-04-27 against `web/src/app/redesign/u/[username]/page.tsx` (84-line static placeholder) + `web/src/app/u/[username]/page.tsx:22` (`PUBLIC_PROFILE_ENABLED=false`).
**Owner action needed:** Sign off on the design spec (the redesign's `preview` fixture covers user-own-profile shape, NOT public-profile shape — needs a fresh design pass). Decide cutover sequencing — co-ship with T357 OR build `/redesign/u/[username]` first and rename when T357 lands.
**Current state in code:** Placeholder shows emoji hero + "Public profile is being rebuilt" message. Legacy `/u/[username]` is kill-switched.
**What's missing:**
- New hero (display name, @username, member-since, verification badge).
- Expert badge with organization (from `expert_title` / `expert_organization`).
- Tier expression — plain text per the no-color-per-tier rule.
- Paginated followers/following lists.
- Working report sheet (using existing `PROFILE_REPORT_REASONS` enum at `lib/reportReasons`).
- Working block-from-public action.

**Coupling:** Co-ships with T357 OR ships earlier as `/redesign/u/[username]`. T330 (just-shipped 'hidden' check at line 204-206 of `web/src/app/u/[username]/page.tsx`) AND T331 + T359 (iOS parallel) must all be in place before flipping `PUBLIC_PROFILE_ENABLED=true`.
**Estimated:** Multi-session build. T4 review (cross-surface, security-sensitive — leaks public PII if wrong).

---

## MEDIUM — single-deploy timing call

## T79 — Settings split into 11 sub-routes

**Verified:** 2026-04-27 — `web/src/app/profile/settings/page.tsx` is currently 5,187 lines (was 5,299; some shrinkage from email-notifications subsection deletion). 11 sub-route stub directories exist under `web/src/app/profile/settings/`.
**Owner action needed:** Pick the deploy window. Cross-surface anchor links (Story Task 6 paywall, Bookmarks Task 4 cap banner, Messages Task 8 DM paywall, Notifications Task 5 alerts link, etc.) must all switch in the same deploy.
**Current state in code:** Monolith intact. Stub directories present but inactive.
**Fix:** Single-deploy split. Anchor-redirect rules ensure all `/profile/settings#anchor` cross-surface links keep working. **Don't split partial.**
**Recommendation:** Schedule with T357 cutover window. The redesign cutover already does the equivalent IA shift — the legacy split may be skippable if the redesign replaces the monolith.

---

## HIGH — cross-surface schema/server work

## T25 — No topic/category alerts (publish-time fan-out)

**Verified:** 2026-04-27 against `VerityPost/VerityPost/AlertsView.swift:300` (`manageSubscriptionsEnabled = false`) + grep for `subscription_topics` table (zero hits) + grep for `/api/alerts/subscriptions` (zero hits).
**Owner action needed:** Sign off on the schema (new `subscription_topics(user_id, category_id, created_at)` table + RLS) + the publish-time fan-out trigger. Decision affects DM volume / push delivery cost at scale.
**Current state in code:** `alert_preferences` table exists (per-alert-type channel/frequency only). `breaking_news` is a global blast. No category-level subscription mechanism.
**Fix:** Schema migration → API routes (`/api/alerts/subscriptions` GET/POST) → publish-time trigger on article insert → iOS flag flip + UI (T29 hands the empty-state copy).

## T42 — iOS data export uses old direct-insert path

**Verified:** 2026-04-27 against `VerityPost/VerityPost/SettingsView.swift:2557-2566` (direct insert into `data_requests` table).
**Owner action needed:** Decision: route iOS through `/api/account/data-export` for parity (web does), or accept iOS divergence?
**Current state in code:** iOS bypasses the rate-limit / audit / dedupe / permission gating that web's API route enforces.
**Fix:** Once owner approves: route iOS through the API endpoint. Load existing rows so pending state survives relaunch.

## T43 — iOS can't see/cancel pending deletion while signed in

**Verified:** 2026-04-27 against `VerityPost/VerityPost/SettingsView.swift:2504-2529` + `Models.swift:5-60` (no `deletion_scheduled_for` field on iOS user model).
**Owner action needed:** UX flow design — should iOS expose a countdown timer + cancel button, or stay with the current "log back in to cancel" message?
**Current state in code:** Delete flow shows a confirmation, submits the request, displays "Request submitted… log back in to cancel." Pending deletion isn't visible while signed in.
**Fix:** Add `deletion_scheduled_for` to iOS VPUser model (decode from server). Surface countdown banner above settings. Add cancel action via `/api/account/delete` with `?action=cancel`.

---

## SEQUENCING NOTES

- **T0.1, T0.2, T0.3, T0.5 — apply ASAP.** Production is broken right now (Family signup, threaded replies, push cron, kid content visibility). All four are SQL-only and reversible. Apply in any order; no dependencies between them.
- **T345 (Confirm-email + cohort reconciliation)** must clear before AUTH-MIGRATION ships. AUTH-MIGRATION pulls in T22, T23, T200, T252, T320, T321 cleanups.
- **T347 (state-flag enum)** is a multi-PR sweep; can land independently of AUTH-MIGRATION but easier to reason about post-migration.
- **T2.7 idempotency** ships before any other billing-touching change to avoid compounding webhook re-run damage.
- **T0.4 / T2.1 / T2.7 paired-tests** — code halves can ship behind a flag any time; flag-flip waits for owner test on a known-recoverable real card.
- **T357 (web cutover)** unblocks T358 (iOS port) and T360 (autonomous component build). T334 caller-side rolls inside T357. T4.8 redesign TS errors must clear before T357 cutover.
- **T363 (public profile rebuild)** must ship + T330 + T331 + T359 (iOS hidden audit) all in place before `PUBLIC_PROFILE_ENABLED` flips.
- **T55 (drop orphan table)** is safe in any window — pre-flight guard prevents accidents.
- **T3.2 deep-link bundle decision** unlocks the iOS parent advance/graduate UI; requires T3.4 (Universal Links — owner Apple Dev console step in TODO-PRE-LAUNCH).

---
