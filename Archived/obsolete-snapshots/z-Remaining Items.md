# z-Remaining Items

Generated 2026-04-16. Everything still outstanding across web, iOS, DB, ops, admin, and copy — pulled from the phase log, AUTONOMOUS_FIXES.md, 04-Ops/PROJECT_STATUS.md, 03-Build-History/FINAL_WIRING_LOG.md, Second Pass Audit flags, and a direct admin-tree review.

Split into **Human-Only** (requires credentials, portals, or real editorial decisions) and **Autonomous** (AI can do it end-to-end, no external accounts needed).

---

## Human-Only

Things the AI cannot do. All of these gate launch or require outside accounts.

### Database & environment
1. **Apply migration 036** (`036_ios_subscription_plans.sql`) in Supabase SQL Editor — seeds `plans.apple_product_id` for the 8 paid SKUs.
2. **Apply migration 037** (`037_user_push_tokens.sql`) in Supabase SQL Editor — creates the push-token table + 2 RPCs the APNs pipeline needs.
2a. **Apply migrations 038, 039, 040, 041, 042, 043, 044, and 045** in Supabase SQL Editor — 038 adds `get_unread_counts()` RPC + covering index for the Pass-4 unread indicator; 039 loosens `message_receipts_select` to owner-OR-sender for the Pass-4 read-receipts "Read" caption; 040 overwrites the placeholder `data_export_ready` email template with real account-essential copy + corrected variables (Pass 5 Task 48); 041 adds `expert_applications.reverification_notified_at` column + seeds `expert_reverification_due` email template + creates `flag_expert_reverifications_due(integer)` RPC for the weekly annual re-verification cron (Pass 5 Task 49); 042 `CREATE OR REPLACE`s `recompute_family_achievements()` to sum across the co-adult (Pass 5 Task 50 — closes the "future enhancement per D34" TODO at `025_phase17_fixes.sql:406–407`); 043 adds `conversations` to the `supabase_realtime` publication so the web + iOS UPDATE channels (Pass-2 Task-10 + Pass-4 Task-44) stop being silent noops (Pass 6 Task 55); 044 adds `users.dm_read_receipts_enabled boolean NOT NULL DEFAULT true` for the D11 follow-up per-user opt-out (Pass 6 Task 62); 045 drops + recreates `bookmarks_insert` RLS as ownership-only, removing the paid-plan gate that contradicted D13's free-tier 10-bookmark allowance (Pass 9 Prompt 2 — Bug 1). All eight are idempotent.
2b. **Apply migrations 046 and 047** in Supabase SQL Editor — 046 adds a generated `articles.search_tsv tsvector` column + GIN index on `articles` so paid search stops doing full-body ILIKE scans (Pass 11 Task 80 — CQ-25 / Bug 70; route migration to `websearch_to_tsquery` deferred); 047 drops + recreates `follows_insert` RLS with an extra `AND public.is_premium()` guard so a direct-client INSERT can't bypass the D28 paid-only follows rule (Pass 11 Task 80 — Bug 76). Both idempotent.

### Apple
3. **Apple Root CA G3** — either `curl` the cert to `site/src/lib/certs/apple-root-ca-g3.der` OR set `APPLE_ROOT_CA_DER_BASE64` in Vercel. Needed for StoreKit JWS + App Store Server Notifications verification.
4. **App Store Connect — create 8 subscription products** matching the IDs in `StoreManager.swift` (4 tiers × monthly/annual at the D42 prices).
5. **App Store Connect — configure V2 Server URL** to `https://veritypost.com/api/ios/appstore/notifications` for both Production and Sandbox.
6. **Apple Developer portal — generate APNs auth key (.p8)**, note the Key ID + Team ID.
7. **Vercel env vars** — set `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_AUTH_KEY` (.p8 PEM contents), `APNS_ENV` (`production` or `sandbox`). `APNS_TOPIC` optional (defaults to `com.veritypost.app`).
8. **Universal links** — publish `apple-app-site-association` on `veritypost.com` so article URLs deep-link into the iOS app.
9. **iOS build verification** — run `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` then `xcodebuild` to confirm clean compile across the 36 Swift files. `xcodegen generate` is clean; `xcodebuild` has never been run end-to-end.

### Stripe
10. **Create 8 live/sandbox prices** in Stripe dashboard matching the 8 paid SKUs.
11. **Run `scripts/check-stripe-prices.js`** to validate, then apply the `UPDATE plans SET stripe_price_id = ...` statements it outputs.

### Content
12. **Publish real articles** — platform has zero published articles today. Editorial team must create them through `/admin/story-manager`. Minimum 10 quiz questions per article (D1 requires this for non-repeating attempts).

### Cosmetic / marketing
13. **Twitter `@site` and `@creator` handles** — social meta config decision.

---

## Autonomous

AI can do all of these. Grouped by priority.

### Launch-critical autonomous work

None. Every launch blocker is in the Human-Only list above. The code is wired and verified.

### Polish gaps flagged in phase log / audits

Small items that aren't launch blockers but are visible rough edges.

~~14. **iOS "you are muted" pre-submit banner** — server blocks correctly, iOS has no inline warning before the user types. Web has this.~~ — resolved 2026-04-16 (Task 41 — `StoryDetailView.swift` composer now fetches `users.is_banned/is_muted/mute_level/muted_until` on mount; renders a red appeal banner with web-parity copy + hex palette when active; Link opens `<siteURL>/appeal`)
~~15. **Login-time cancel-deletion hook** — user logs back in during delete grace window, deletion should cancel. Not wired.~~ — resolved 2026-04-16 (Task 59 — NEW `site/src/app/api/account/login-cancel-deletion/route.js` bearer-auth endpoint for iOS; inline `createServiceClient().rpc('cancel_account_deletion', ...)` calls in `api/auth/login/route.js` + `api/auth/callback/route.js`; iOS `AuthViewModel.login()` POSTs via `cancelDeletionOnLogin(accessToken:)`. Zero schema change — leans on existing `027_phase19_deletion.sql` RPC + service-role grant. Signup + session-restore paths deliberately not wired)
~~16. **Admin identity-verification UI** for data export requests (`/admin/verification` or similar — route/flow exists server-side, UI surface missing).~~ — resolved 2026-04-16 (Task 51 — NEW `/admin/data-requests/page.js` + 3 API routes under `requireRole('editor')`; approve flips `identity_verified=true/_at/_by` + `processed_by` to unblock the `claim_next_export_request` cron gate; reject writes timestamped reason to `notes`; red `Legal hold` pill suppresses both actions when `legal_hold=true`; wired into admin hub Users & Identity group by Task 53)
~~17. **Seed `data_export_ready` email template** — template row not in DB; the export cron sends but has nothing to render.~~ — resolved 2026-04-16 (Task 48 — migration `01-Schema/040_data_export_email_template.sql` overwrites the placeholder body on the pre-existing seeded row with account-essential copy + corrected `variables` jsonb; one-line addition to `send-emails/route.js` TYPE_TO_TEMPLATE closes the hidden second root cause where the cron was silently skipping every `data_export_ready` notification regardless of template content)
~~18. **Co-adult family model** in `recompute_family_achievements` — RPC currently assumes single parent, not 2 adults.~~ — resolved 2026-04-16 (Task 50 — migration `01-Schema/042_family_achievements_coadult.sql` `CREATE OR REPLACE`s the RPC with a single-block `v_members` graft that appends co-adult user_ids via `subscriptions.family_owner_id = v_owner_id AND s.status = 'active'`; mirrors the shape already used by `family_members()` in `017_phase9_family.sql`; every other line byte-identical, idempotent, deterministic, sticky `earned_at` preserved)
~~19. **`/u/[username]` generateMetadata** — no OG tags on the public profile page.~~ — resolved 2026-04-16 (Task 19 — new `site/src/app/u/[username]/layout.js` with server-side `generateMetadata`, reuses `/card/[username]/opengraph-image`, private/missing profiles return `robots: noindex`)
~~20. **Home page metadata** — generic, needs proper title/description/OG.~~ — resolved 2026-04-16 (Task 20 — root `site/src/app/layout.js` metadata enriched with `metadataBase`, brand title, quiz-gate description, `openGraph`, `twitter`; page location shifted from client-component `page.js` to root `layout.js`)
~~21. **Phase log is stale** — captures Tasks 1–8 of the autonomous wiring session but not 9–11. Swift count also stale.~~ — resolved 2026-04-16 (Task 18 — Tasks 9/10/11 summaries folded into `Autonomous Production Wiring` section; Swift count was already corrected in Pass 1 Task 17)

### Deferred from Task 11 verification

Explicitly flagged out of scope during the autonomous session.

~~22. **iOS `StoreManager.purchase` — pass `.appAccountToken(UUID(uuidString: userId))`** so App Store Server Notifications can correlate to a user without depending on sync having run first. Eliminates the "orphan notification" case.~~ — resolved 2026-04-16 (Task 42 — `StoreManager.purchase(_:)` now passes `.appAccountToken(session.user.id UUID)` as a StoreKit option; fallback to no-option purchase if session missing or id fails UUID parse; `appleReceipt.js` reads the field automatically through the existing JWS parser)
~~23. **Per-token sandbox/production APNs dispatch** — currently one `APNS_ENV` per deploy. If a single deploy serves both TestFlight-sandbox and App Store, needs per-token env check.~~ — resolved 2026-04-16 (Task 43 — `apns.js` exports new `resolveApnsEnv()`; `withApnsSession/sendApnsAlert/sendPushToUser` parametrised; cron `send-push` partitions dispatch into `{production, sandbox}` buckets and runs one session per non-empty bucket; `APNS_ENV` retained as fallback)
~~24. **`REFUND_REVERSED` handling** on App Store Server Notifications.~~ — resolved 2026-04-16 (Task 21 — added to `HANDLED_TYPES` and dispatched on the existing SUBSCRIBED-style reactivation arm in `api/ios/appstore/notifications/route.js`)
~~25. **iOS `MessagesView` realtime parity** — Task 10 fixed web only. iOS `MessagesView` likely has the same no-realtime gap.~~ — resolved 2026-04-16 (Task 44 — iOS `MessagesView` + `DMThreadView` now have three Supabase realtime subscriptions mirroring web: open-conversation messages INSERT with id-dedup, conversations UPDATE patch + re-sort, `conversation_participants` INSERT reload; unique channel-name-per-mount via unix timestamp; teardown via `.task(id:)` cancellation)
~~26. **Unread indicator on `/messages`** via `conversation_participants.last_read_at` — schema ready, UI never wrote/read the field.~~ — resolved 2026-04-16 (Task 45 — migration `01-Schema/038_messages_unread.sql` adds `get_unread_counts()` RPC + covering index; web + iOS both call it on cold load, render a black/VP.accent pill + bold row when `unread > 0`, write `last_read_at = now()` on conversation open, increment unread live via a cross-convo `messages` INSERT channel)
~~27. **Read receipts via `message_receipts`** — table exists, entirely unused.~~ — resolved 2026-04-16 (Task 46 — migration `01-Schema/039_message_receipts_rls.sql` loosens `message_receipts_select` to owner-OR-sender; web + iOS both upsert receipt rows on conversation open after `last_read_at`, load own-message receipts for cold-open render, and live-flip the caption via a `message_receipts` INSERT realtime channel; iMessage-style single "Read" caption under the last read own-message on both platforms)
~~28. **`/admin/verification` surface journalist `background_check_status`** — API route exists (`.../clear-background`), admin page doesn't show the field.~~ — resolved 2026-04-16 (Task 22 — left-column list now shows a colored `<BgBadge>` pill for journalist applications, amber pending / green cleared; detail-pane button was already wired)
~~29. **Annual expert re-verification cron** — Blueprint 2.4 requires it, no entry in `vercel.json`.~~ — resolved 2026-04-16 (Task 49 — migration `01-Schema/041_expert_reverification.sql` adds `reverification_notified_at` dedup column + upserts `expert_reverification_due` email template + creates `flag_expert_reverifications_due(p_warning_days integer DEFAULT 30)` RPC. NEW `site/src/app/api/cron/flag-expert-reverifications/route.js` mirrors `sweep-kid-trials`. NEW `vercel.json` cron entry `30 4 * * 1` (Mon 4:30am UTC). `send-emails` TYPE_TO_TEMPLATE addition. Amber `ReverifyBadge` in `admin/verification/page.js` next to existing `BgBadge` when `credential_expires_at` is within 30 days. No auto-revoke per spec + D3)
30. **Behavioral anomaly detection** — Blueprint 10.3 specifies it (impossible reading speed, rapid-fire quiz answers, identical comment text, rapid follow/unfollow). No table, no RPC, no detection path.

### Second Pass Audit flags (not fixed)

~~31. **`profile/page.js` Activity tab** reads v1 `quiz_attempts` columns (`score`, `total`, `passed`).~~ — resolved 2026-04-16 (Task 23 — `profile/page.js` was already v2, audit flag was stale; actually-broken `profile/[id]/page.js` stats + recent-activity blocks rewritten to group per-answer rows by `(article_id, attempt_number)`)
~~32. **`profile/page.js` Achievements tab** not wired to `user_achievements`.~~ — resolved 2026-04-16 (Task 25 NO-OP — recon confirms `loadAchievements` at `profile/page.js:315–349` already queries `achievements` + `user_achievements` correctly; FINAL_WIRING_LOG Task 10 self-report was right, Second Pass Audit's "not wired" claim was incorrect)
~~33. **Quiz block renders on articles with 0 questions** — gate on pool size is there but the block shell is still visible.~~ — resolved 2026-04-16 (Task 24 NO-OP — `currentUser && quizPoolSize >= 10` gate at `story/[slug]/page.js:471` mobile and `:494` desktop already hides the entire block; no friendly-error shell outside the gate)
~~34. **Modal focus trapping** (Interstitial, kids PIN) — WCAG gap, Tab can escape. Broader refactor.~~ — resolved 2026-04-16 (Task 61 — NEW `site/src/lib/useFocusTrap.js` hook (records trigger, auto-focuses first focusable, wraps Tab/Shift+Tab, optional Escape callback, restores focus on close; onEscape stabilised behind an internal ref to prevent parent-re-render focus thrash). Applied cross-sweep per PM-approved Option C to all 6 web modal surfaces: `Interstitial.jsx`, `LockModal.jsx`, `kids/profile/page.js` exit-PIN, `messages/page.js` new-message search, `story/[slug]/page.js` regwall, `story/[slug]/page.js` report. Every panel now carries `role="dialog"` + `aria-modal="true"` + a labelling attribute. Regwall Escape intentionally inert. iOS out of scope — native SwiftUI modal chrome handles focus + keyboard correctly. Definitive close per PM directive — no deferred follow-up)
~~35. **Kid-facing "my stats" view** — `/profile/kids/me` was deleted as redundant, but a kid-styled stats view is a gap. Kid profile now shows stats via `/kids/profile` (Task 8), so this may be resolved — verify.~~ — resolved 2026-04-16 (Task 57 — recon found iOS parity gap: web `/kids/profile` rendered 4 stat tiles (Articles / Day streak / Badges / Verity Score), iOS `KidProfileView` rendered 3 (missing Verity Score). D2/D7 permits own-score display — this was iOS-side omission, not privacy drift. Fix landed iOS-only: added `verityScore` @State + scoped `kid_profiles.verity_score` fetch in `KidProfileView.load()`, refactored 3-column HStack into a 2x2 LazyVGrid of 4 `StatBubble`s. Kid-mode isolation confirmed on both platforms)

### Admin tree issues (from my review)

~~36. **Broken link in admin hub** — `/admin/expert-queue` 404s (real route is `/expert-queue`, user-facing).~~ — resolved 2026-04-16 (Task 12)
~~37. **Duplicate entry in admin hub Previews group** — "Article Manager (Dark Theme)" links to the same `/admin/story-manager` as the Content Pipeline entry. Stale.~~ — resolved 2026-04-16 (Task 13)
~~38. **`admin/roles/page.js` out of sync** (stale `ADMIN_PAGES` list, missing 12 v2 admin pages, "stories" labels).~~ — resolved 2026-04-16 (Task 15 — page deleted entirely)
~~39. **Two parallel RBAC surfaces** — `admin/roles/page.js` (hardcoded) vs. `admin/permissions/page.js` (DB-backed).~~ — resolved 2026-04-16 (Task 15 — broken hardcoded surface deleted; `admin/permissions/` is now the single canonical RBAC admin surface, wired into the hub by Task 17a)
~~40. **`admin/features/page.js` — 20+ "story" labels** in admin copy, plus `story-page` section id.~~ — resolved 2026-04-16 (Task 14 — 14 user-visible labels + section id converted to article/article-page)

### Repo hygiene

~~41. **Old admin tree at `site copy/src/app/admin/`** still has `notes/` (D15 cut) and `credibility/` (D27 cut) folders.~~ — resolved 2026-04-16 (Task 16 — entire `site copy/` directory deleted, 517 MB)
~~42. **`Verity_Post_Migration_Gameplan.md` is referenced as a project doc** but the file doesn't exist in the project root.~~ — resolved 2026-04-16 (Task 26 — references removed from Phase Log reference-doc list and from PROJECT_STATUS.md §1 intro, table row, and follow-up item 17)
~~43. **One remaining TODO** at `site/src/app/api/email/send-test/route.js:1`.~~ — resolved 2026-04-16 (Task 27 — deleted the route file, its route dir, the empty `api/email/` parent, plus the admin caller: `sendTest` handler + "Send Test" button from `admin/email-templates/page.js`. Repo TODO count in `site/src/` is now 0)

### Previously deferred — worth revisiting

~~44. **AI-assisted recap generation** — admin button wires to `/api/ai/generate`; output still needs manual review before publish. Acceptable but flag whether this should become auto-run.~~ — resolved 2026-04-16 (Task 58 — decision recorded: keep AI generation manual, PM default, D3 editorial-integrity rationale. Already the state. Item-44 framing correction: `/api/ai/generate` handles article body + kids rewrite + timeline, NOT weekly recap (`admin/recap/page.js:7` explicit comment: "AI-generated questions are a later build"). All 4 AI-assisted admin surfaces (story-manager, kids-story-manager, pipeline, recap) are already human-in-the-loop with editor-gate. RSS `feeds.is_auto_publish` + `feeds.is_ai_rewrite` both default false. Zero AI crons. No code change needed)
~~45. **Email delivery worker** — Resend is wired, `send-emails` cron runs. Phase log called this "deferred, actually resolved." Confirm it's running and delivering.~~ — resolved 2026-04-16 (Task 56 DONE — verified working end-to-end: `/api/cron/send-emails` every 10 min with `CRON_SECRET` + `RESEND_API_KEY` gates, `email.js` pure fetch to Resend with no TODO stubs, skip-on-unverified + alert-prefs opt-out + missing-template branches all present, `TYPE_TO_TEMPLATE` includes Pass-5 additions `data_export_ready` (L17) + `expert_reverification_due` (L18). Only residual dependency is the pending migration-apply in item 2a — not a worker defect)

### New findings surfaced in Pass 2 (2026-04-16)

~~46. **`App Only/app/` drift directory** — 7 stale Swift copies (`ContentView.swift`, `VerityPostApp.swift`, `Push*.swift`, `StoreManager.swift`, `Keychain.swift`). Same drift pattern as the `Shared/app/` directory we deleted in Task 28 — the real files live in `VerityPost/VerityPost/`; these copies will silently misrepresent state. Should be deleted (or quarantined during Pass 3 reorg) and the `App Only/README.md` turned into a pointer to the real paths.~~ — resolved 2026-04-16 (Task 33 — drift directory deleted outright per Pass 3 spec's sole delete-don't-quarantine exception; 7 Swift duplicates gone; `App Only/README.md` re-homed to `02-Parity/iOS-Only.md` with inline title + opening-sentence updates)
~~47. **`profile/[id]/page.js` v1 `story_id` column references** — `reading_log` and comments reads still query the v1 `story_id` column name. No user-visible breakage today (queries likely return empty or silently fail), but it's v1 leftover. Low-urgency autonomous fix: rename to `article_id` where the v2 schema uses that.~~ — resolved 2026-04-16 (Task 38 NO-OP — Pass 2 Task 23's stats + recent-activity rewrite already dropped the `story_id` selects as a side effect; grep confirms zero `story_id` across `site/src/app/profile/`)
~~48. **`Site Only copy/` directory** — flagged by Pass 2 as a duplicate, not in spec scope. Should be quarantined or deleted in Pass 3 reorg. (Already on the Pass 3 plan; tracking here for completeness.)~~ — resolved 2026-04-16 (Task 37 — quarantined as `99-Archive/site-only-copy-snapshot/`)
~~49. **`admin/permissions/page.js` lacks grant-CRUD UI** — the page's own footer explicitly says "To move perms between sets, grant/revoke sets to roles/plans, or grant a set directly to a user, use the Supabase SQL editor for now. A full CRUD UI is a later pass." Not urgent (admins who create new roles / sets are a rare operation; SQL is fine short-term), but worth tracking as an explicit future item.~~ — resolved 2026-04-16 (Task 52 — executed across 4 CHECKPOINT sub-phases: 52a Sets tab + membership + admin role-gate bugfix; 52b Role/Plan grant matrices; 52c User grants with server-side ILIKE search + optional `expires_at` / `reason`; 52d Registry create + delete + footer removal. Single-file tabbed UI; `admin/permissions/page.js` grew 205 → 1011 LOC. All 6 sub-surfaces landed; direct-client RLS writes throughout; optimistic-with-rollback; no schema changes, no new API routes, no new migrations)

### New findings surfaced in Pass 4 (2026-04-16)

~~50. **DM read-receipt privacy toggle** — today every viewer of a conversation unavoidably emits `message_receipts` rows when they open the thread (Task 46). Polish: let users opt out of emitting receipts (their view action stops producing `read_at` rows; senders stop seeing "Read" for messages this user reads). Deferred — not launch-critical; follow the pattern of existing alert-preference toggles in `SettingsView` / `/profile/settings/alerts`. Suggested shape: a `users.dm_read_receipts_enabled` boolean (default true) + a `SettingsView` + `/profile/settings/…` row + a client-side gate on the `markVisibleMessagesAsSeen` / `upsert` step on both platforms.~~ — resolved 2026-04-16 (Task 62 — migration `01-Schema/044_dm_read_receipts_enabled.sql` adds `users.dm_read_receipts_enabled boolean NOT NULL DEFAULT true`. Web: `/messages` extends `me` SELECT + gates the Task-46 receipt upsert on the flag; `/profile/settings/data` adds 5th `PRIVACY_ROWS` entry wired through `togglePrivacy`. iOS: `DMThreadView` adds `loadDmReceiptsPref` + early-return gate on `markVisibleMessagesAsSeen`; `SettingsView.DataPrivacyView` gains new `Messages` section with Toggle, `.task` load + `.onChange` save. Client-side gate only — documented social convention (Signal/iMessage/WhatsApp parity), not a security boundary)
~~51. **`conversations` publication coverage** — `reset_and_rebuild_v2.sql:6487–6500` adds `messages`, `message_receipts`, `conversation_participants`, `kid_sessions`, `notifications`, `perms_global_version` to `supabase_realtime` but NOT `conversations`. Both Pass-2 Task-10 (web) and Pass-4 Task-44 (iOS) subscribe to `conversations` UPDATE events; if the live DB relies on Supabase's default catch-all publication behaviour this works, but the miss is surprising. Either add `ALTER PUBLICATION supabase_realtime ADD TABLE conversations;` in a one-line migration or document the intentional omission. Low urgency — the list-reorder-on-new-message path still works via the `conversation_participants` INSERT handler + full reload.~~ — resolved 2026-04-16 (Task 55 — migration `01-Schema/043_conversations_realtime_publication.sql` lands the `ALTER PUBLICATION supabase_realtime ADD TABLE conversations;` inside the same `DO $$ ... EXCEPTION WHEN OTHERS THEN NULL END $$;` idempotent DO-block pattern as `reset_and_rebuild_v2.sql:6487–6500`. Recon confirmed Supabase does NOT auto-populate the publication — the miss was genuine, not a default-catch-all case. Observable symptom pre-apply: counterparty-sent DMs left the recipient's conversation list with stale preview + timestamp until next reload (unread pill still updated via Pass-4 Task-45 cross-convo channel). No client code change required — both platforms already subscribe correctly)

### New findings surfaced in Pass 5 (2026-04-16)

~~52. **User-facing data export bypasses the server async pipeline.** `site/src/app/profile/settings/data/page.js:149–156` runs a client-side JSON export and inserts a `data_requests` row with `status: 'completed'`, not `status: 'pending'`. The server-side async pipeline (`claim_next_export_request` cron + signed-URL bundle + `data_export_ready` notification + email) is fully wired — including the Pass-5 Task-48 email template fix and the Pass-5 Task-51 admin identity-verification surface — but no rows ever enter the pending queue for the cron to claim. Admin data-requests queue today only surfaces completed rows (client-side self-exports) as an audit trail. Rewiring the user-facing button to insert `status: 'pending'` and wait on the cron is a small change that activates the full pipeline. Polish / async-correctness work; safe to defer. Deliberately not fixed in Task 51 to keep scope bounded.~~ — resolved 2026-04-16 (Task 60 — web-only: `profile/settings/data/page.js` `handleDownload` → `handleRequestExport` — dropped the client-side 5-table fetch + Blob download + `status='completed'` insert; replaced with a single `{ user_id, type: 'export' }` insert relying on schema defaults (`status='pending'`, `identity_verified=false`, `regulation='gdpr'`). UI now shows a green `we'll email you a secure download link` confirmation post-submit. Byte-identical insert shape to iOS `SettingsView.DataPrivacyView.requestExport` (iOS was already correct — surprise find during recon))

### New findings surfaced in Pass 6 (2026-04-16)

~~53. **iOS settings deletion flow bypasses `schedule_account_deletion` RPC.** `VerityPost/VerityPost/SettingsView.swift:1135–1144` `DataPrivacyView.requestDeletion()` directly inserts a `data_requests` row with `type='deletion'` instead of POSTing to `/api/account/delete` (which calls the `schedule_account_deletion` RPC, see `027_phase19_deletion.sql:39–79`). Without the RPC call, `users.deletion_requested_at` + `users.deletion_scheduled_for` never get set, so `sweep_expired_deletions()` never picks up the row. Web correctly POSTs to `/api/account/delete`. Surfaced during Task 60 cross-platform recon; deliberately out of scope for a data-export task. Fix shape: replace the direct insert with a bearer-auth POST to `/api/account/delete` using the session access token — same pattern as Task 59's `cancelDeletionOnLogin`. Safe to defer — iOS-initiated deletions would silently fail to sweep today, but users who log back in cancel normally via Task 59's hook anyway.~~ — resolved 2026-04-17 (Pass 13 Task A — two-edit checkpoint sequence. Web: `site/src/app/api/account/delete/route.js` gained an inline `resolveUser(request)` helper that branches on `Authorization: Bearer` → `createClientFromToken` else falls through to the cookie-backed `createClient()`; `requireAuth` import dropped in favour of `authClient.auth.getUser()` to match the `/api/account/login-cancel-deletion` pattern exactly and avoid an unneeded RLS round-trip on the `users` profile table; applied to both `POST` and `DELETE` handlers; `DELETE` signature now takes `request`. Cookie path for the existing web caller at `profile/settings/data/page.js:282` unchanged. iOS: `SettingsView.swift` `DataPrivacyView.requestDeletion()` now fetches the Supabase session, POSTs `{}` to `<siteURL>/api/account/delete` with `Authorization: Bearer <accessToken>` and `Content-Type: application/json`, flips `deleteSubmitted` only on 2xx, logs on non-2xx or throw. Pattern parity with `AuthViewModel.swift:428–439 cancelDeletionOnLogin` + 22 other Bearer call sites in the iOS app. **Open follow-up:** silent-failure-on-delete — a non-2xx response swallows into `Log.d` with no user-facing surface. Preserved per PM directive (matches existing `requestDeletion` + `requestExport` behaviour); tracked as a future UX polish item, not an Item-53 regression.)

---

## How to sequence this

1. **Before launch:** items 1–12 (Human-Only) block cutover. Add items 31–34 (audit flags) and 22–24 (Task 11 deferred) for a clean launch feel.
2. **After soft launch, before marketing push:** items 28, 29, 19, 20 (public-facing polish).
3. **Post-launch:** items 26, 27, 30 (features that need real usage data to tune).
4. **Anytime:** admin cleanup (36–40), repo hygiene (41–43), phase log update (21). These are cheap autonomous passes.
